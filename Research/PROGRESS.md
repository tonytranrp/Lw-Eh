# Lw-Eh Progress Ledger

This is the durable, git-tracked ledger for the autonomous build loop. Updated at the end of every firing. Git history is ground truth if this ever drifts — see `git log --oneline`.

- **Phase:** embedded-validation (unchanged — example/tooling cleanup, no new library feature)
- **Last completed:** Firing 19 closed out the signal_core architectural question and explicitly recommended a fresh orientation pass rather than assuming a default next step. Took that literally: read through the whole repo tree fresh rather than defaulting to the standing RISC-V/ARM/AVR toolchain note, and found something genuinely stale that four firings of size-scaling investigation had walked right past — **`examples/riscv32_esp_minimal/main.cpp`, `examples/arm_cortex_m_minimal/main.cpp`, and `examples/avr_minimal/main.cpp` were all still their original Phase-0 stub content**: a dummy `button_event`, a `signal<button_event,4>` that never got anything attached to it, and a literal `TODO(Phase 1+): ... once signal<> is implemented` comment — even though `signal<>` was implemented back in firing 3 (17 firings ago) and has since been extensively tested, measured, and codegen-verified. This went unnoticed for so long specifically *because* no RISC-V/ARM/AVR toolchain has ever been available on this machine to build them and force the staleness into view — unlike `esp32_minimal/`, which got caught up to reality in firing 7 the moment it was actually cross-compiled for the first time.
  Reviewed each architecture's `CMakeLists.txt`, linker script, and startup stub by careful source-level reading before touching anything (the only verification method available without a toolchain) — all three were already structurally sound and honestly scoped ("link-only, not flashable" documented consistently), including the ASM/C-language-enabling fix from `esp32_minimal`'s own firing-7 bug already being correctly applied to all four architectures in `examples/CMakeLists.txt`, not just Xtensa. Confirmed the Xtensa-specific `.literal*`-before-`.text*` linker-script fix (also firing 7, an `l32r` backward-only-literal-addressing quirk) correctly does *not* appear in `riscv32.ld`/`cortex_m.ld` — neither architecture's addressing mode needs it, so its absence there is correct, not a missed fix.
  The actual fix: moved the shared `scenario.hpp` fixture from `examples/esp32_minimal/` to `examples/` directly (`git mv`, clean rename) — it was nested under one architecture's directory purely as a Phase-0 accident, and having `riscv32_esp_minimal/` reach into a sibling `esp32_minimal/` directory for its own usage logic would have been a stranger dependency than fixing the location. Updated its own doc comment to describe its now-genuinely-shared scope, and updated the three include sites that referenced the old path (`examples/esp32_minimal/main.cpp`, `size_audit/with_lweh_main.cpp`) plus wired all three previously-stale examples to it for real, matching `esp32_minimal/main.cpp`'s exact call pattern (`run_scenario(2, 42, true, 50, -60)`) — `app_main()` for RISC-V/ARM (matching their startup stubs' entry-point convention), plain `main()` with a trailing `for(;;){}` for AVR (matching avr-libc's own crt0-calls-main convention, already correctly different in the pre-existing stub).
- **Current measured size:** Unchanged — **375 bytes incremental** for 5 event types on real ESP32 Xtensa hardware, reconfirmed after the move (`xtensa-esp32-elf-size`, 2026-07-16) — a pure file-location/include-path change has no codegen impact, as expected, and the remeasurement confirms it.
- **Test status:** 7/7 pass (`ctest --preset host-debug`, 2026-07-16) — unaffected (change scoped to `examples/`/`size_audit/`, disjoint from `tests/`'s fixtures), run anyway per established practice. Host (`host-debug`), host size-probe (`size-host`), and the real Xtensa cross-compile (`esp32-xtensa`) all build clean under the full flag set. **Honest limitation, unchanged from every prior firing**: the RISC-V/ARM/AVR example fixes themselves could **not** be verified by actually compiling them — no toolchain for any of the three is present on this machine (reconfirmed absent as of firing 14/19's checks). Verification for those three is limited to careful source-level review and structural consistency with the now-proven-correct Xtensa pattern (identical `run_scenario()` call shape, identical include mechanics, entry-point convention matched to each architecture's own already-reviewed startup stub) — real confirmation has to wait for whenever a toolchain becomes available, exactly the same honest caveat this ledger has carried since Phase 0's own environment note.
- **Next up:** open, no strong candidate identified yet — the two threads that dominated recent firings (signal_core sizing, and now the stale-examples cleanup) are both closed out. Standing candidate: RISC-V/ARM/AVR toolchains remain the largest unresolved gap, needs explicit user permission to install and isn't being sought since it isn't blocking anything above. Next firing should do a genuinely fresh orientation pass (this firing's own approach — read the whole tree, don't assume — paid off once already; worth repeating rather than defaulting to the toolchain note out of habit).
- **Open questions for the user:** none blocking. Same standing note: RISC-V/ARM/AVR toolchains not present on this machine; installing one is the user's call, not something this loop will do unilaterally.
- **Subagents launched this project so far:** 11 total (unchanged — this firing was direct source review and mechanical example fixes, no fresh design question needed a subagent).
  1. C++ design/architecture research for a tiny event system → became research.md Part A.
  2. CMake/compiler-flags research for embedded zero-dependency builds → became research.md Part B.
  3. Phase 0 library header/API architecture proposal → adopted into ARCHITECTURE.md.
  4. Phase 0 CMake/test/tooling architecture proposal → adopted into ARCHITECTURE.md (one suggestion rejected).
  5. `delegate<Sig>` toolchain/GCC-version portability investigation → confirmed safe; flagged avr-gcc 7.3.0 as lowest-margin (still safe); flagged ICF folding isn't automatic on Lw-Eh's target toolchains without `-flto`/gold/lld.
  6. `delegate<Sig>` EnTT cross-validation / language-correctness investigation → confirmed the two-overload `bind` design; surfaced the `std::invoke` footgun now baked into the implementation.
  7. `signal<Event,N>` reentrancy: EnTT's actual `sigh`/`sink` source investigation → found EnTT's real removal mechanism is swap-and-pop (not shift-down), traced a confirmed double-invoke + UB-OOB hazard for detaching not-yet-visited peers, surveyed ETL/QP/Linux-kernel prior art for the mark-and-skip pattern.
  8. `signal<Event,N>` reentrancy: adversarial stress-test of the proposed mark-and-skip design → compiled+ASan/UBSan-verified every reentrancy scenario safe, established the exact attach-during-dispatch deferral contract now documented in `signal.hpp`.
  9. `intrusive_signal<Event>` reentrancy: adversarial stress-test of the proposed singly-linked head-insertion design → confirmed self-detach/detach-other/attach-during-dispatch all safe, but found and precisely characterized a real compound-reentrancy bug (self-detach + detach-own-frozen-successor), now documented as a known caveat rather than engineered around.
  10. `signal_core` deferral pressure-test (firing 19): given real firing-16/17/18 measured data, does the Phase-0 amendment's own revisit trigger apply now? → no, budget headroom is comfortable; recommended deferring the full rewrite while flagging `attach()`'s uncompensated duplication as real.
  11. Narrower `attach()`/`detach()`-only shared-core design investigation (firing 19): can the Event-agnostic slot-scan be factored out without the full event-erasure adapter-stub cost the original design needed? → yes, a `slot_view`-based runtime-count finder is technically sound (with AVR Harvard-architecture and strict-aliasing caveats addressed); also identified the per-(Event,MaxListeners,Fn) duplication axis; proposed the noinline experiment as a cheaper first test, which the loop then ran directly and found to regress size by 62 bytes, decisively ruling it out.

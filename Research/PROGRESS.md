# Lw-Eh Progress Ledger

This is the durable, git-tracked ledger for the autonomous build loop. Updated at the end of every firing. Git history is ground truth if this ever drifts — see `git log --oneline`.

- **Phase:** embedded-validation (unchanged — this firing was test coverage + a documentation-consistency fix, no functional/behavioral library code)
- **Last completed:** Two small, distinct, unrelated changes, deliberately committed separately rather than bundled (see rationale below):
  1. **Implemented last firing's concrete "next up"**: `capacity_boundary_test.cpp` now pins down that the *implicit* default (`lweh::signal<Event>` with no explicit second template argument) is really `default_max_listeners = 4` from `config.hpp`, not just "some capacity." Every test that previously relied on the implicit default (`attach_detach_test.cpp`, `multi_event_isolation_test.cpp`) never attached more than 2 listeners, so none of them would have caught a silent change to that constant. Added the mirror image of the file's existing explicit-N=2 boundary test: attach exactly 4, confirm a 5th is rejected, confirm all 4 (not the 5th) fire. This closes out the "audit an already-implemented type's test suite for entangled-vs-isolated / silently-unverified gaps" vein that's been productive for four firings running (delegate equality, intrusive_signal head/middle detach, signal<> middle-slot reuse+order, and now this) — as flagged two firings ago, that vein is now genuinely exhausted for the existing type surface; there's no more low-hanging fruit of this specific shape left to find by inspection.
  2. **Found and fixed something different while investigating what to do next**: read `include/lweh/lweh.hpp` (the umbrella header) end-to-end for the first time this loop and noticed it includes `detail/signal_core.hpp`. Traced the history: `ARCHITECTURE.md`'s *original* Phase 0 directory tree explicitly planned `signal.hpp` as "a thin shim over `detail::signal_core`" — but that plan was deliberately amended during Phase 0 itself (documented in `ARCHITECTURE.md`'s own amendment section and cross-referenced accurately in `signal.hpp`'s class-level comment: "deliberately deferred, not abandoned"). The stub file itself, `signal_core.hpp`, was never updated to match — it still read "TODO(Phase 1): implement," directly contradicting `signal.hpp`'s own accurate account of the same type's status. Confirmed via grep that nothing in the library actually uses `lweh::detail::signal_core` (it's genuinely empty and unreferenced outside `lweh.hpp`'s include and `CMakeLists.txt`'s header listing). Rewrote the stub's comment to match `signal.hpp`'s framing exactly and cite the amendment, rather than deleting a deliberately-preserved placeholder for a future revisit. **Committed separately from item 1 above** (`git log` will show two commits this firing, not one) — they're unrelated in kind (a new test vs. a documentation-accuracy fix on unrelated dead code found along the way), and bundling them into one commit would have made each harder to review independently for no benefit; splitting cost nothing since both were already independently green.
- **Current measured size:** Unchanged — still **520 bytes incremental** on real ESP32 Xtensa hardware (firing 8/10/11). Neither change this firing touched compiled logic: item 1 is test-only, item 2 is a comment-and-whitespace-only edit to an empty, unused class (confirmed via grep before touching it) — no re-measurement needed or taken.
- **Test status:** 7/7 pass (`ctest --preset host-debug`, 2026-07-16), full suite run after each of the two changes. Item 2 rebuilt 5 of 7 test targets (every one that includes the `<lweh/lweh.hpp>` umbrella rather than a specific header) — confirms the dependency graph is exactly as understood: `delegate_test.cpp`/`intrusive_signal_test.cpp` include specific headers directly and correctly did NOT rebuild.
- **Next up:** the "audit existing types for silent gaps" vein is now genuinely exhausted (see above), so the next firing needs a different angle. Two honest candidates, neither yet investigated in depth — flagging both rather than picking blind:
  1. **The size-scaling claim itself is under-validated.** research.md §A10 frames the whole budget as "shared engine + small marginal per-event-type cost, not a naive flat cap" — a core, load-bearing claim — but the only real measurement on record is one aggregate number (520 bytes for 3 event types total, split across both storage policies). Nobody has actually measured the *marginal* cost of a 4th, 5th, 6th event type to confirm the trend is really small-and-roughly-constant rather than something that only looked fine at n=3. Extending `examples/esp32_minimal/scenario.hpp` with 1-2 more `signal<>`-backed event types and re-measuring per-type delta on real Xtensa hardware would be a genuine, structural, size-relevant firing (full build+measure, not just host tests) — this is probably the more valuable of the two candidates since it validates a claim the whole project's value proposition rests on, not just internal consistency.
  2. RISC-V/ARM/AVR toolchains remain the standing, largest unresolved gap. Reconfirmed absent from `~/.platformio/packages/` as of two firings ago; installing one needs explicit user permission and isn't being sought since it isn't blocking anything else.
  No strong preference stated here on purpose — next firing should actually read scenario.hpp and the current size_audit output before committing to (1), rather than this note assuming its shape in advance (the same discipline that paid off the last four firings).
- **Open questions for the user:** none blocking. Same standing note: RISC-V/ARM/AVR toolchains not present on this machine; installing one is the user's call, not something this loop will do unilaterally.
- **Subagents launched this project so far:** 9 total (unchanged — this firing was direct investigation-by-reading-the-actual-code plus test-writing and a documentation fix; no fresh design question needed a subagent).
  1. C++ design/architecture research for a tiny event system → became research.md Part A.
  2. CMake/compiler-flags research for embedded zero-dependency builds → became research.md Part B.
  3. Phase 0 library header/API architecture proposal → adopted into ARCHITECTURE.md.
  4. Phase 0 CMake/test/tooling architecture proposal → adopted into ARCHITECTURE.md (one suggestion rejected).
  5. `delegate<Sig>` toolchain/GCC-version portability investigation → confirmed safe; flagged avr-gcc 7.3.0 as lowest-margin (still safe); flagged ICF folding isn't automatic on Lw-Eh's target toolchains without `-flto`/gold/lld.
  6. `delegate<Sig>` EnTT cross-validation / language-correctness investigation → confirmed the two-overload `bind` design; surfaced the `std::invoke` footgun now baked into the implementation.
  7. `signal<Event,N>` reentrancy: EnTT's actual `sigh`/`sink` source investigation → found EnTT's real removal mechanism is swap-and-pop (not shift-down), traced a confirmed double-invoke + UB-OOB hazard for detaching not-yet-visited peers, surveyed ETL/QP/Linux-kernel prior art for the mark-and-skip pattern.
  8. `signal<Event,N>` reentrancy: adversarial stress-test of the proposed mark-and-skip design → compiled+ASan/UBSan-verified every reentrancy scenario safe, established the exact attach-during-dispatch deferral contract now documented in `signal.hpp`.
  9. `intrusive_signal<Event>` reentrancy: adversarial stress-test of the proposed singly-linked head-insertion design → confirmed self-detach/detach-other/attach-during-dispatch all safe, but found and precisely characterized a real compound-reentrancy bug (self-detach + detach-own-frozen-successor), now documented as a known caveat rather than engineered around.

# Lw-Eh Progress Ledger

This is the durable, git-tracked ledger for the autonomous build loop. Updated at the end of every firing. Git history is ground truth if this ever drifts — see `git log --oneline`.

**Format note (firing 37):** `Last completed` holds only the most recent firing's summary, not a running narrative — every prior firing's full detail (what was found, why, what was verified, exact commands/output) is preserved verbatim in its own commit message(s), which is where cross-references and future digging should go. This entry had grown to four stacked multi-paragraph essays (firings 34-36) before this trim; nothing in them was lost, they're all still in `git log`.

- **Phase:** hardening
- **Format note (this firing):** trims the narrative section again — same rhythm as before, roughly every 5-8 new bullets. Nothing lost; commit hashes below have the full detail.
- **Last completed:** A real ESP32 board enabled genuine on-silicon validation for the first time (firings 38-46), requiring 4 real hardware-only bugs to be found and fixed (IRAM0 word-only access, `esptool.py elf2image`'s `AT>`-blind LMA/VMA handling, a `call0`-into-windowed-ABI crash + the watchdog it unmasked). Since then: every core API surface plus the capacity-boundary edge case confirmed on real silicon; `flash.py` landed as the tested flash+observe recipe; fresh-clone reproducibility confirmed across host presets, ESP32 hardware, `size_audit/`, and `packaging_check/`'s `find_package` path (surfacing and fixing a dead `CMAKE_C_COMPILER` cache variable, and a `LWEH_BUILD_SIZE_AUDIT`-defaults-OFF reproducibility gap fixed via a new `esp32-xtensa-size` preset); real GitHub Actions CI history checked directly (67/67 runs green) and a real `actions/checkout@v4→v7` deprecation warning fixed; the project's first-ever compiler-warning coverage (`-Wall -Wextra -Wpedantic -Wshadow -Wconversion -Wsign-conversion`) added, catching one real variable-shadowing bug, verified clean on Clang/real Xtensa GCC/real CI GCC; a direct textual grep confirmed research.md's banned-facility list is genuinely honored in `include/lweh/`; `-ffreestanding` added to the three genuine `-nostdlib` toolchains; and `size_report.py`'s own usage docstring caught twice, for real, missing `--control-target` (silently producing 898 instead of 888) and a stale preset reference — both fixed and reconfirmed at exactly 888.
  Documentation/tooling hardening: README's two Usage snippets, both `size_report.py` recipes, and `packaging_check/`'s install+consume recipe all confirmed to actually run as literally documented, not just read as prose (the host-representative `size_report.py` recipe reports 1084 bytes — expected, host CRT overhead vs. bare-metal Xtensa, not meant to match). Two new permanent CI lint gates added, matching `hermeticity-lint`'s "cheap grep/compile gate protecting an invariant" pattern: `version-consistency-lint` (guards the 4 hand-maintained `0.1.0` strings against future drift) and `unbuildable-examples-syntax-check` (host-compiles the RISC-V/ARM/AVR placeholder examples' `.cpp`+`.c` source, since no real toolchain exists to verify them any other way — previously zero compile-time verification of any kind). `.clangd`/`.gitattributes` checked (both correct); `.gitignore` gained `.vscode/`/`.idea/` entries.
  Two items remain flagged, not fixed, since both are public-facing release markers rather than ordinary development commits this loop's standing authorization covers: the repo's GitHub description has a typo, and no git tag exists anywhere despite `0.1.0` being declared in three manifests — both ready to apply on a one-line go-ahead, see Open questions.
- **This firing: checked whether research.md's own illustrative "Companion toolchain-file stub" example (§B, ~line 599) has drifted from the real, evolved `cmake/toolchains/xtensa-esp32.cmake` now that `-ffreestanding` has been added there — it has, and that's correctly not a bug.** The stub still shows only `set(CMAKE_CXX_FLAGS_INIT "-mlongcalls")`, one flag short of the real file's `"-mlongcalls -ffreestanding"`. Applied this project's own already-established principle (from the earlier full `research.md` re-read, Research/PROGRESS.md commit history): research.md is explicitly framed as pre-implementation reference material ("Status: Pre-implementation research... reviewed before architecture/implementation decisions are locked in"), and this specific block is labeled a "stub" — illustrative, not a promise of permanent sync with the real, evolved implementation, which correctly lives in `cmake/toolchains/xtensa-esp32.cmake` itself (cross-referenced from ARCHITECTURE.md/PROGRESS.md). No edit made — recording this as a concrete, checked instance of that general principle rather than silently either ignoring the discrepancy or inappropriately "fixing" a document that's deliberately not meant to track post-implementation changes.
- **This firing: a comprehensive "is everything still green together, right now" sweep across the full accumulated infrastructure churn (new CMakePresets.json preset, new warning flags, `-ffreestanding`, three new CI jobs) — each verified individually before, never together on one HEAD.** Fresh-reconfigured and rebuilt all three host presets from a clean `build/`: `host-debug` (14/14 build steps, 7/7 tests, 0.70s), `host-asan` (14/14, 7/7, 1.28s), `host-gcc` (14/14, 7/7, 1.15s) — all clean, zero warnings. ESP32 side: fresh `esp32-xtensa-size` reconfigure+build (10/10 steps clean), `size_report.py` reconfirmed the incremental figure at exactly **888 bytes**, byte-for-byte matching the long-established number — real confirmation the entire infra sweep introduced zero size regression. One genuine hiccup investigated rather than assumed broken: `xtensa-esp32-elf-g++` wasn't found on this fresh Bash shell's `PATH` at first — not a regression, just this environment's already-documented per-shell-invocation `PATH` re-export requirement (durable memory) reasserting itself in a brand-new shell.
  Live-hardware reconfirmation via `flash.py` did **not** complete this firing: 12 consecutive `esptool` failures, all the identical "Wrong boot mode detected (0x13)," across three different invocations (`write_flash`, `chip_id`, `erase_flash` — including the documented erase-first mitigation for boot-loop-degraded auto-reset). Actively ruled out the two known causes before giving up: no `PermissionError` at any point (rules out concurrent-thread COM5 contention, see [[project_lw-eh-concurrent-firings]]) and `Get-PnpDevice` confirms the CP210x bridge still enumerates fine on COM5 (rules out a dropped/disconnected board). This doesn't affect confidence in current HEAD's correctness — the exact same code path was already confirmed working on real silicon multiple times earlier this session (`LWEH BOOT`/`OK`/`DETACH OK`/`CAPACITY OK`, including a 120+-second stable-idle capture), and nothing in this firing's changes touch runtime behavior at all (pure infra/tooling). Treating this as session-specific hardware flakiness worth a fresh retry next firing, not a blocker — see durable memory for the full data point.
- **This firing: closed the one gap last firing's syntax-check job left unaddressed — the RISC-V/ARM/AVR examples' `CMakeLists.txt` files themselves, never once parsed by CMake in this project's history, now empirically verified as valid.** `examples/CMakeLists.txt`'s own gating logic only `add_subdirectory()`s each architecture's folder when `CMAKE_SYSTEM_PROCESSOR` matches exactly (confirmed by reading it) — meaning `riscv32_esp_minimal/`, `arm_cortex_m_minimal/`, and `avr_minimal/`'s own `CMakeLists.txt` files have never been read by CMake at all, since none of their real toolchains exist here. Verified all three anyway, without a real toolchain: wrote scratch-only (never committed) toolchain files that fake `CMAKE_SYSTEM_PROCESSOR` to trigger the right `add_subdirectory()` branch while using the real host clang underneath purely to reach CMake's *configure* step. All three configured cleanly — genuine, first-ever confirmation these files are syntactically and semantically valid CMake. (The subsequent *build* step, tried for completeness, fails exactly as expected and for an unrelated reason: RISC-V's `startup.S` is real, non-portable assembly a host x86 assembler can't parse — `j 1b` read as an ambiguous x86 mnemonic, not a real problem, just proof assembly can't be host-verified this way, consistent with why the syntax-check job never attempted `.S` files either.)
  Deliberately **not** turned into permanent CI infrastructure, matching this project's own established precedent (firing 24's compile-fail-testing evaluation: a narrow, stable, low-regression-risk check is better served by a one-time recorded verification than by permanently maintaining fake, no-real-toolchain-behind-them CMake files as CI state): these 3 files are extremely simple and have been frozen since firing 20, and the more likely regression source — a future `scenario.hpp`/library-header change breaking one of these examples' actual source — is already permanently covered by last firing's `unbuildable-examples-syntax-check` job.
- **This firing: found and fixed a genuine correctness bug in the library's actual logic — the first one this entire session that isn't tooling/docs/CI.** `intrusive_link`/`intrusive_node<Event>` (`include/lweh/detail/intrusive_core.hpp`, `intrusive_signal.hpp`) never deleted copy/move operations, despite their own doc comment explicitly citing Boost.Intrusive's hooks as precedent. Empirically confirmed the hazard before touching anything: compiled and ran a repro where copy-*assigning* an unattached node onto an already-linked one compiles cleanly (zero warnings) and silently overwrites the linked object's own state in place — `sig.publish()` afterward invokes what looks like a listener that was never `attach()`'d at all, since the corrupted object is still the one the signal's internal list actually points to. Dispatched one subagent to verify the fix against real prior art before writing it (not guessed): confirmed Boost's actual hooks (real source, not docs) deliberately leave copy/move permissive only because hook types must stay usable inside STL containers — a reason that doesn't apply here (Lw-Eh never uses STL containers) — and confirmed no real intrusive-list implementation (Boost, ETL, Linux kernel `list_head`) solves relocation via move-constructor syntax at all (a move ctor structurally can't see the owning container to relink); Boost's own real answer is a separately-named `swap_nodes()`. The subagent also caught a critical, easy-to-miss subtlety: deleting all four special members without also re-adding an explicit `intrusive_link() = default;` would silently make every derived listener type non-default-constructible too (a deleted special member still counts as "user-declared" for the implicit-default-constructor-suppression rule), breaking every existing test/example — confirmed this exact failure mode by trying it, then fixed it with the explicit default ctor.
  Implemented exactly as verified: 4 explicit deletions + 1 explicit default constructor on `intrusive_link`, propagating automatically to `intrusive_node<Event>` and every concrete derived listener with no other file needing to change. Re-ran the original repro after the fix: both the copy-construct and copy-assign lines now fail to compile with a clear, correctly-attributed error chain (not just "operator overload not found"). Full re-verification, not spot-checked, since this changes a public header's structural contract: `host-debug` and `host-asan` both clean-reconfigured/rebuilt, 7/7 pass on each, zero warnings; real ESP32 Xtensa (`esp32-xtensa-size` preset) clean-reconfigured/rebuilt, zero warnings, `size_report.py` reconfirmed **exactly 888 bytes incremental** — zero size/codegen impact, exactly as predicted (nothing anywhere in this repo ever copied/moved these types, so no such code was ever being emitted either way). Deliberately did not add permanent compile-fail CI infrastructure for this specific check (unlike `hermeticity-lint`/`version-consistency-lint`/`unbuildable-examples-syntax-check`, which each guard a broad surface any unrelated future edit could silently drift): this is a narrow, now-thoroughly-documented, four-line invariant sitting directly beside the doc comment explaining exactly why it exists, closer to firing 24's "one-time recorded verification, not permanent infrastructure" precedent than to the host-asan precedent — the one-time verification above is that record. Pushed (`7854acd`) and watched the real CI run complete: all 5 jobs green, including `host-asan` (real sanitizer coverage on Ubuntu) and `host-gcc` (real GNU GCC, unlike this machine's Clang-aliased `g++`) — the two environments this fix's correctness claims most needed independent confirmation from, not just this machine's Clang.
- **This firing: a direct follow-up to the `intrusive_link` fix — does the same copy/move-while-linked hazard class exist anywhere else in the library, given how much of this session's recent scrutiny went into tooling/CI/docs rather than a fresh, adversarial re-read of the core headers themselves?** Re-read `delegate.hpp`, `signal.hpp`, `detail/signal_core.hpp`, and `lweh.hpp` end-to-end, specifically hunting for the same class of bug (a type whose implicit copy/move silently desynchronizes some external structure from the object's own copied-by-value state) rather than re-checking what prior audits already covered (warnings, banned facilities, ABI/size). Confirmed clean, and for a specific, checkable reason rather than just "looks fine": `delegate<Sig>` is two plain pointers with zero ownership or external back-references — copying it is exactly as safe as copying an `int`. `signal<Event,N>`'s storage is a self-contained array of `delegate`s with nothing outside the object ever pointing *into* it — copying a `signal<>` produces a genuinely independent, valid duplicate with its own copy of the same bindings, unlike `intrusive_signal<>` where external listener nodes' own list-position identity is what made copying dangerous. `detail::signal_core` is a deliberately empty, unimplemented stub (nothing to hazard-check). This confirms the bug fixed last firing was the *only* place this hazard class could exist in the current codebase — the intrusive/externally-referenced structure `intrusive_link` uniquely has is what created the risk, not a pattern repeated elsewhere. Also skimmed `tests/test_harness.hpp` for the first time this session while in this mode: simple, hosted-C++ free functions and a static counter, no lifetime concerns, nothing to find.
- **Current measured size:** **888 bytes incremental** for 6 event types on real ESP32 Xtensa hardware (`xtensa-esp32-elf-gcc` 8.4.0, `-Os -flto -flto-partition=none` + full aggressive flag set including `-ffreestanding`), confirmed both via the `esp32-xtensa-size` preset's `size_audit/size_report.py` output (with the correct `--control-target`, now also fixed in the script's own docstring) and via actual flash+boot (`LWEH CAPACITY OK` on real silicon). Comfortably within the 1-2KB budget for 6 event types per research.md A10.
- **Test status:** 7/7 pass locally under `host-debug`/`host-asan`/`host-gcc` (all three, fresh-reconfigured from a clean `build/` this firing — `host-gcc` locally is still a Clang alias on this machine, not real GNU GCC, but is separately confirmed green on real CI). ESP32 Xtensa (`esp32-xtensa-size` preset) rebuilt clean this firing; incremental size reconfirmed at exactly 888 bytes. Live-hardware reconfirmation attempted but not completed this firing (12 consecutive non-contention `esptool` failures, see above) — last *successful* real-hardware confirmation remains the detach/capacity-boundary milestone earlier this session.
- **Next up:** genuinely open, on the most complete footing yet — every core API surface plus the capacity-boundary edge case has run correctly on real hardware, on top of source-level, test-suite, codegen, research-doc, packaging, and CI-infrastructure checks all coming back clean. The project is at a natural completion point for everything achievable without new toolchain access. The one remaining substantive gap — RISC-V/ARM/AVR (and real ESP-IDF) toolchains have never been installed, only source/config-reviewed — stays correctly gated on the user's explicit go-ahead (large downloads, PATH/environment modification), not something this loop can or should decide unilaterally: if/when approved, that's the highest-value next unit of work by a wide margin; absent that, this loop keeps watching for anything genuinely new rather than manufacturing busywork in an already-complete area.
- **Open questions for the user:** none blocking, plus one small, low-stakes, ready-to-apply item this loop deliberately did not act on unilaterally (public GitHub metadata, not a repo file): the repo's GitHub description has a typo — currently **"Light weight Event handlers for small circuts."** — proposed fix: **"Light-Weight Event Handler: a from-scratch, zero-dependency, header-only C++ event/signal dispatch library for embedded targets."** (matching `library.json`'s own wording), optionally with topics like `cpp`, `embedded`, `esp32`, `header-only`, `event-driven` added for discoverability. Say the word and this loop will apply it via `gh repo edit`; otherwise it's a nice-to-have, not blocking anything. Second item, same category (a public release marker, not routine commit history): no git tag exists anywhere (local or remote) despite `library.json`/`idf_component.yml`/`config.hpp` all already saying `0.1.0` — say the word and this loop will create+push an annotated `v0.1.0` tag against current `main`; until then, README's own "actively under development, not yet frozen" status stands as-is, untagged. Standing notes carried forward: the machine's global `http.postbuffer=500MB` git setting can still cause push OOM errors under concurrent memory pressure (workaround: per-command `-c http.postbuffer=` override, documented in durable memory, never edit the persisted config); RISC-V/ARM/AVR toolchains not present on this machine, installing one is the user's call.
- **Subagents launched this project so far:** 26 total.
  26. `intrusive_link` copy/move-safety investigation: given an empirically-confirmed real bug (copy-assigning an unattached node onto an already-linked one silently corrupts the linked object's state), is deleting all four copy/move special members the right, complete fix, and does real prior art (Boost.Intrusive, ETL, Linux kernel `list_head`) do this differently? → yes, delete all four, confirmed against real Boost.Intrusive source (its own hooks stay copyable only for STL-container compatibility, a reason that doesn't apply to Lw-Eh) and ETL/kernel precedent (no real implementation solves relocation via move-constructor syntax; Boost's actual answer is a separately-named `swap_nodes()`, structurally impossible to replicate as an implicit move here since a move ctor can't see the owning container). Caught one critical implementation subtlety before code was written: the four deletions would also suppress the implicit default constructor unless an explicit `intrusive_link() = default;` is added back, which would have silently broken every existing test/example's listener construction.
  1. C++ design/architecture research for a tiny event system → became research.md Part A.
  2. CMake/compiler-flags research for embedded zero-dependency builds → became research.md Part B.
  3. Phase 0 library header/API architecture proposal → adopted into ARCHITECTURE.md.
  4. Phase 0 CMake/test/tooling architecture proposal → adopted into ARCHITECTURE.md (one suggestion rejected).
  5. `delegate<Sig>` toolchain/GCC-version portability investigation → confirmed safe; flagged avr-gcc 7.3.0 as lowest-margin (still safe); flagged ICF folding isn't automatic on Lw-Eh's target toolchains without `-flto`/gold/lld.
  6. `delegate<Sig>` EnTT cross-validation / language-correctness investigation → confirmed the two-overload `bind` design; surfaced the `std::invoke` footgun now baked into the implementation.
  7. `signal<Event,N>` reentrancy: EnTT's actual `sigh`/`sink` source investigation → found EnTT's real removal mechanism is swap-and-pop (not shift-down), traced a confirmed double-invoke + UB-OOB hazard for detaching not-yet-visited peers, surveyed ETL/QP/Linux-kernel prior art for the mark-and-skip pattern.
  8. `signal<Event,N>` reentrancy: adversarial stress-test of the proposed mark-and-skip design → compiled+ASan/UBSan-verified every reentrancy scenario safe, established the exact attach-during-dispatch deferral contract now documented in `signal.hpp`; kept permanently standing via the `host-asan` preset since firing 21, and via CI since firing 28.
  9. `intrusive_signal<Event>` reentrancy: adversarial stress-test of the proposed singly-linked head-insertion design → confirmed self-detach/detach-other/attach-during-dispatch all safe, but found and precisely characterized a real compound-reentrancy bug (self-detach + detach-own-frozen-successor), now documented as a known caveat rather than engineered around.
  10. `signal_core` deferral pressure-test (firing 19): given real firing-16/17/18 measured data, does the Phase-0 amendment's own revisit trigger apply now? → no, budget headroom is comfortable; recommended deferring the full rewrite while flagging `attach()`'s uncompensated duplication as real.
  11. Narrower `attach()`/`detach()`-only shared-core design investigation (firing 19): can the Event-agnostic slot-scan be factored out without the full event-erasure adapter-stub cost the original design needed? → yes, a `slot_view`-based runtime-count finder is technically sound (with AVR Harvard-architecture and strict-aliasing caveats addressed); also identified the per-(Event,MaxListeners,Fn) duplication axis; proposed the noinline experiment as a cheaper first test, which the loop then ran directly and found to regress size by 62 bytes, decisively ruling it out.
  12. `requires`-clause technical correctness investigation (firing 23): exact syntax, C++17 fallback exactness, binary-size impact, and interaction with the existing `is_member_pointer_v` static_assert → syntax confirmed correct and verified empirically against real clang compilation, but found `is_invocable_r_v` has its own INVOKE-based loophole matching the file's own documented `std::invoke`-avoidance rationale, and that a `requires`-clause failure pre-empts/hides the friendlier body `static_assert` message via SFINAE — directly shaped the final implementation.
  13. Concept-constraint value/minimalism evaluation (firing 23): is this worth doing at all, benchmarked explicitly against firing 19's `signal_core` deferral precedent → no, recommended a plain C++17 `static_assert(is_invocable_r_v<...>)` instead of `requires`, and removing `LWEH_HAS_CONCEPTS` as dead cruft with no other plausible future use found.
  14. Compile-fail testing mechanics investigation (firing 24): what's the correct, robust way to test "this snippet must fail to compile with this message" on this exact CMake/Ninja/Clang/Windows setup → found `try_compile()` has a real, empirically-confirmed header-dependency-tracking gap that would have silently broken the whole mechanism; delivered a complete, tested, ready-to-use alternative (direct compiler invocation via a `cmake -P` driver), not used to build permanent infrastructure but its verified compiler-invocation recipe was reused directly for that firing's one-time manual verification.
  15. Compile-fail testing value evaluation (firing 24): is permanent CTest infrastructure justified for 3 narrow, zero-incidence checks → no, benchmarked against both the `host-asan` precedent (worth building) and the `signal_core` precedent (correctly deferred) and landed closer to the latter; recommended a one-time, dated, recorded verification instead, which the loop then performed for real.
  16. GCC 8.4.0 IPA-ICF vtable-folding-asymmetry investigation (firing 34): given real disassembly showing byte-identical `on_event()` overrides folded to one address while byte-identical `invoke_erased()` overrides did not, in the same binary — why the difference? → built a faithful repro on the real toolchain with `-fdump-ipa-icf-details`, confirmed empirically (via the dump's `"Equals called for:"` log lines, not guessed): ICF hash-buckets candidates before ever comparing bodies, `on_event`'s instantiations hash identically and fold, `invoke_erased`'s hash differently and never reach body comparison, because its call to `on_event` is polymorphic/indirect (hashed more richly, type-leaking) vs. `publish()`'s direct call to `dispatch()` (hashed thin, folds fine) — a clean internal control group ruled out simpler theories. Not fixable without collapsing the erasure-thunk into the listener's own virtual method (a real architecture tradeoff for ~tens of bytes); no code change made, matches pre-registered scoping.
  17. ESP32 image/flashing mechanics investigation (firing 38): given real hardware + esptool.py + prebuilt Arduino-ESP32 bootloader/partition files but no ESP-IDF/pio CLI, what's the exact `elf2image`/`write_flash` recipe to produce a genuinely bootable image, and critically whether the second-stage bootloader hard-requires an ESP-IDF-style app-descriptor header a from-scratch app wouldn't have → no descriptor required (confirmed via `nm` on the real local bootloader ELF, the checking code isn't even linked in); full verified recipe + a real landmine caught (stale prebuilt `default.bin`) delivered, see commit `f4a09c6`.
  18. Real-hardware startup-code correctness investigation (firing 38): does `scenario.hpp`'s global `signal<>`/`intrusive_signal<>` object graph actually need `.bss` zeroing/`.data` copy-down/C++ static-init to behave correctly on real silicon (not just "is it bad practice"), and is the current linker script's flat IRAM-only memory layout structurally viable for real execution → zero dynamic init needed (confirmed via `nm`), but the flat layout is genuinely broken (a real byte-store to IRAM0 confirmed via disassembly, would fault on real silicon); built, linked, and verified a fix (real IRAM0/DRAM0 split, LMA≠VMA `.data`). Implemented and independently re-verified, landed in commit `e438462`.
  19. Real-hardware bootloader boot-loop diagnosis (firing 38): the flash succeeds completely (all 4 components hash-verified) and the ROM correctly loads the prebuilt second-stage bootloader, but it hangs silently before producing any log output and the RTC watchdog resets it in a loop — why? → in flight, result pending; has direct access to the real hardware to test hypotheses, not just reason abstractly.
  20. ESP32 bare-metal UART0 TX register investigation (firing 39): exact base address/FIFO/status-register layout for polled UART0 TX, and whether clock/GPIO-matrix/baud pre-init is required from a cold from-scratch entry point (no SDK) → base `0x3ff40000`, FIFO at `+0x0`, status/`TXFIFO_CNT` at `+0x1C` bits `[23:16]`, `CLKDIV` at `+0x14` (all cited to real local `soc/*_reg.h` headers, cross-checked via `nm` against the real bootloader ELF's own `UART0` absolute symbol — exact match); no pre-init needed, UART0 is clocked/pin-routed/~115200-8N1 from power-on reset on this specific boot path (ROM + bootloader already rely on exactly this). Implemented directly, verified via objdump/literal-pool decoding against the real built ELF.
  21. ESP32 bootloader silent-hang root-cause research (firing 40): given the real captured boot log (loads, jumps to entry, ~9s silence, RTC watchdog reset, repeats), what does real ESP-IDF v4.4.7 bootloader source do between entry and its first log line, and what's the most likely from-scratch-flashing-specific cause → found this exact prebuilt bootloader's logging is entirely compiled out (`CONFIG_BOOTLOADER_LOG_LEVEL_NONE=y`), so silence alone was never diagnostic; the ~9s timing matches the watchdog's fresh 9000ms re-arm partway through `bootloader_init()`, meaning the hang is later than a naive reading suggested; ranked SPI flash-mode mismatch (image header consumed at the flash-init step) as the leading cause. Independently reproduced and confirmed locally (no hardware touched): `elf2image` on `bootloader_dio_40m.elf` without an explicit `--flash_mode` defaults to `QIO` against DIO-only board wiring, with an entry-point address matching the real captured boot log exactly; regenerating with explicit `--flash_mode dio --flash_freq 40m --flash_size 4MB` produces a verified, materially different, ready-to-test image. Not yet tried on real hardware — COM5 was actively held by a separate concurrent session throughout.
  22. Xtensa `entry`/`retw` ISA-level cross-validation (independent thread, before commit `b6db5a7` landed): is "`app_main()` never returns" a genuine architectural fix for the `call0`-into-windowed-ABI return crash, or just symptom-masking? → genuine fix, confirmed via real ISA-level citations (Espressif's own `WindowedOption.tex` pseudocode, ESP32's actual `core-isa.h`/`xtensa_vectors.S`): `entry` unconditionally rotates the register window from whatever `PS.CALLINC` holds with no check a matching windowed `callX` preceded it, and `call0` never sets `PS.CALLINC` — so this is architecturally the *only* broken transition in the whole call graph, not one of several. Real window-exception-vector setup was evaluated and deliberately not pursued (nothing needs `app_main` to return). Landed via commit `38ca015`.
  23. Systematic ESP32 codegen deep-verification sweep (this session): first full struct-layout/RTTI/ICF/gc-sections check on the compiled binary since firing 34, predating the entire real-hardware saga — is there anything the disassembly shows that source-level reasoning missed? → all clean, real `objdump`/`readelf`/`nm` evidence throughout: `delegate` confirmed exactly 8 bytes/zero padding on the real Xtensa ABI; `intrusive_core::dispatch()` confirmed a genuinely shared symbol across event types via real `call8` call sites; zero RTTI/exception artifacts leaked in despite the aggressive flag set; ICF folding behaves exactly as firing 34 characterized (reconfirmed with a new event-type pairing); `gc-sections` left nothing suspicious; the incremental size figure reconciled exactly, byte-for-byte, across every section. This is the investigation that flagged `detach()`/`unlink()` as never having been compiled into any ESP32 binary, directly motivating the detach() and capacity-boundary hardware-confirmation work that followed (commits `732ed60`, `52ac413`).
  24. ARM Cortex-M bug-transfer investigation: do any of the four real ESP32-Xtensa hardware bugs (IRAM0 word-only access, esptool.py's `AT>`-blind `elf2image`, call0-into-windowed-ABI, RTC watchdog) apply to the Cortex-M placeholder, and is anything else architecturally relevant? → none of the four transfer (confirmed via real ARM docs: Code region is byte-accessible, standard flashing tools honor `AT>`/LMA correctly, no windowed-register concept exists) — but surfaced a genuinely more fundamental gap unprompted: the placeholder's `ENTRY(Reset_Handler)` only sets ELF metadata; real hardware needs an actual `.isr_vector` array at address 0, which doesn't exist yet. Landed as a comment in `cortex_m.ld`.
  25. AVR bug-transfer investigation: same question for the AVR placeholder → none of the four transfer either (true Harvard split via separate instructions rather than a permission bit, `PROGMEM` confirmed unused anywhere in this repo, avr-libc's crt1 + avr-gcc's own linker script are decades-proven, no windowed-register concept), but surfaced AVR's own non-Xtensa watchdog gotcha (avr-libc FAQ: WDT can survive a reset at a short timeout) worth flagging, plus confirmed the project's existing ISR/concurrency-out-of-scope policy (research.md §A9) bites harder in practice on AVR's 8-bit-native width. Landed as a comment in `avr_minimal/main.cpp`.

# Lw-Eh Progress Ledger

This is the durable, git-tracked ledger for the autonomous build loop. Updated at the end of every firing. Git history is ground truth if this ever drifts — see `git log --oneline`.

- **Phase:** embedded-validation (unchanged — this firing was host-only test coverage, no library/build code)
- **Last completed:** Followed up on last firing's own "next up" list with real checks rather than acting on assumption, per the lesson that firing itself established:
  1. **Checked whether `intrusive_signal`/`intrusive_node` have any equality/comparison operator analogous to `delegate::operator==`** (candidate (b) from last firing). Read `intrusive_core.hpp` directly: `unlink()` matches by reference identity (`&node`), not by reconstructed value — there is no `operator==` anywhere in the intrusive types, and there shouldn't be one; identity-based attach/detach is the whole point of the intrusive design (a node either *is* the one you're pointing at, or it isn't). Correctly a dead end, not a gap — recording this so a future firing doesn't re-propose it.
  2. **Read through all 6 existing `intrusive_signal_test.cpp` cases looking for what they actually exercise** and found a real, narrow, genuine gap instead: no test directly detaches a **middle** node (neither head nor tail) from a 3+-node list, and no test cleanly isolates detaching the **head** node via an ordinary, non-reentrant call — every existing head-removal case (tests 3 and 6) is entangled with self-detach-during-dispatch reentrancy, which exercises `unlink()`'s `head_==&node` branch only as a side effect of a different, more complex scenario. Added tests 7 and 8: test 7 attaches A, B, C (C ends up head), detaches C directly from outside any callback, confirms B becomes the new head and B/A still dispatch correctly in order; test 8 attaches A, B, C, detaches B (the true middle — neither head nor tail), confirms C now points directly to A and both still fire correctly while B does not. Both passed on the first try, confirming the linked-list relinking reasoning was correct going in.
  3. **Checked whether `signal<Event,N>`'s array-based `detach_matching` has an analogous position-sensitivity gap.** Read `signal.hpp` directly: it doesn't — `detach_matching` is a uniform linear scan that just clears `slots_[i] = slot_t{}` on a match; there's no relinking surgery, so a slot's position in the array (first/middle/last) isn't a structurally distinct code path the way a linked list's head/middle/tail is. Correctly ruled out, not deferred.
  4. **Checked whether README's claimed integration points actually exist** (`library.json`, `integrations/esp-idf/`, `cmake/toolchains/{arm-cortex-m,avr,riscv32-esp,xtensa-esp32}.cmake`) rather than assuming the README is accurate. All confirmed present on disk.
  5. **Re-checked `~/.platformio/packages/` for any AVR/ARM/RISC-V toolchain that might have been installed since the Xtensa one was discovered** (the same route that found the Xtensa toolchain several firings ago). Only `toolchain-xtensa-esp32/` is present — the RISC-V/ARM/AVR gap is still real, not stale.
- **Current measured size:** Unchanged — still **520 bytes incremental** on real ESP32 Xtensa hardware (firing 8/10/11). This firing added test-only code (never compiled into the library or any measured artifact), so no re-measurement was needed or taken.
- **Test status:** 7/7 pass (`ctest --preset host-debug`, 2026-07-16), full suite run. Judged as worth the full suite despite this being a test-only, single-file-scoped change (per the loop's rules this could have been scoped to just the `intrusive_signal` test) because linked-list relinking bugs are exactly the class of bug that can have subtle, non-local effects if the reasoning was wrong — cheap insurance (under a second) for a data structure with a documented, already-narrow reentrancy caveat.
- **Next up:** two concrete candidates, both informed by this firing's "check, don't assume" checks rather than guesses: (a) apply the same direct-branch-coverage audit just completed for `intrusive_signal` to `signal<Event,N>`'s own test suite (`attach_detach_test.cpp`, `capacity_boundary_test.cpp`) — specifically check whether there's a test confirming a detached slot's index gets correctly reused by a subsequent `attach()` (the mark-and-skip/first-fit-empty-slot scan in `signal.hpp`'s `attach()` implies this should just work, but per this firing's own lesson, that should be confirmed by reading the actual test file before asserting it either way, not assumed); (b) RISC-V/ARM/AVR toolchains remain the standing, largest unresolved gap, reconfirmed this firing still genuinely absent from this machine (not just stale documentation) — installing one would need explicit user permission per the safety rules and hasn't been sought, correctly, since it isn't blocking any of the work above. No strong preference between the two; (a) keeps momentum on the pattern that already paid off twice this firing and the firing before it.
- **Open questions for the user:** none blocking. Same standing note: RISC-V/ARM/AVR toolchains not present on this machine; installing one is the user's call, not something this loop will do unilaterally.
- **Subagents launched this project so far:** 9 total (unchanged — this firing, like the one before it, was direct investigation-by-reading-the-actual-code-and-tests plus test-writing against already-decided, already-verified semantics; no fresh design question needed a subagent).
  1. C++ design/architecture research for a tiny event system → became research.md Part A.
  2. CMake/compiler-flags research for embedded zero-dependency builds → became research.md Part B.
  3. Phase 0 library header/API architecture proposal → adopted into ARCHITECTURE.md.
  4. Phase 0 CMake/test/tooling architecture proposal → adopted into ARCHITECTURE.md (one suggestion rejected).
  5. `delegate<Sig>` toolchain/GCC-version portability investigation → confirmed safe; flagged avr-gcc 7.3.0 as lowest-margin (still safe); flagged ICF folding isn't automatic on Lw-Eh's target toolchains without `-flto`/gold/lld.
  6. `delegate<Sig>` EnTT cross-validation / language-correctness investigation → confirmed the two-overload `bind` design; surfaced the `std::invoke` footgun now baked into the implementation.
  7. `signal<Event,N>` reentrancy: EnTT's actual `sigh`/`sink` source investigation → found EnTT's real removal mechanism is swap-and-pop (not shift-down), traced a confirmed double-invoke + UB-OOB hazard for detaching not-yet-visited peers, surveyed ETL/QP/Linux-kernel prior art for the mark-and-skip pattern.
  8. `signal<Event,N>` reentrancy: adversarial stress-test of the proposed mark-and-skip design → compiled+ASan/UBSan-verified every reentrancy scenario safe, established the exact attach-during-dispatch deferral contract now documented in `signal.hpp`.
  9. `intrusive_signal<Event>` reentrancy: adversarial stress-test of the proposed singly-linked head-insertion design → confirmed self-detach/detach-other/attach-during-dispatch all safe, but found and precisely characterized a real compound-reentrancy bug (self-detach + detach-own-frozen-successor), now documented as a known caveat rather than engineered around.

# Lw-Eh Progress Ledger

This is the durable, git-tracked ledger for the autonomous build loop. Updated at the end of every firing. Git history is ground truth if this ever drifts — see `git log --oneline`.

- **Phase:** embedded-validation (unchanged — this firing was documentation, not library/build work)
- **Last completed:** Wrote a real `README.md`, replacing the original two lines that predated all of this project's actual work (deferred as an open option for three firings running). Covers: what Lw-Eh is and why it exists (the "EnTT is too much" framing from `research.md`'s own opening research question), the zero-heap/no-exceptions/no-RTTI/zero-dependency/header-only constraint list, the current real measured footprint (520 bytes on ESP32 Xtensa, cited from last firing with a link to `Research/PROGRESS.md` for the current number rather than duplicating a number that will go stale), a comparison table of the two storage policies (`signal<Event,N>` vs. `intrusive_signal<Event>`) with their real tradeoffs, a real, API-verified-accurate usage example for each (checked the example code directly against the current `signal.hpp`/`intrusive_signal.hpp` source before writing it, not from memory), integration instructions for all four consumption paths already built in Phase 0 (vendor, CMake `add_subdirectory`, ESP-IDF component, PlatformIO), and host build/test instructions. Explicitly states the project is "actively under development, API not yet frozen" rather than presenting it as a finished 1.0 — an honest status given the loop is still adding real API surface. Deliberately left out any mention of the autonomous-loop development process itself: a README describes the artifact for its users, not how it was built (that narrative already lives in `Research/PROGRESS.md`'s own history). Verified every internal link (9 relative paths to `Research/*.md`, `examples/esp32_minimal/scenario.hpp`, `integrations/esp-idf/`, `library.json`, `cmake/toolchains/`, `size_audit/`, `LICENSE`) actually resolves to a real file/directory before committing.
- **Current measured size:** Unchanged from last firing — **520 bytes incremental** on real ESP32 Xtensa hardware for the 3-event-type, both-storage-policy scenario (`size_audit/size_report.py --build-dir build/esp32-xtensa --prefix xtensa-esp32-elf-`, toolchain per `Research/PROGRESS.md`'s memory-recorded path). This firing touched no compiled code, so no new measurement was taken or needed.
- **Test status:** Unchanged from last firing (7/7, `ctest --preset host-debug`) — correctly not re-run this firing. Documentation-only change with zero compiled-code impact; per the loop's own proportionality rule this doesn't need a build or test pass, only link validation (done — see above), which isn't a CTest concern.
- **Next up:** open choice: (a) revisit whether `bloaty` is installable/already-present the same way the Xtensa toolchain turned out to be (flagged two firings running now, still not done); (b) push deeper embedded codegen verification — read `-Wl,--print-gc-sections` output or the `.map` file on the real Xtensa build to confirm dead-code elimination is doing what's expected, matching the loop's own "codegen deep-verification" step, which still hasn't been exercised on real hardware output; (c) resume general feature work — `MaxListeners` capacity is currently fixed at `default_max_listeners = 4` with no per-call-site override demonstrated beyond the boundary test; a real usage example showing a non-default capacity might be worth adding, or investigate whether research.md's originally-scoped feature set has any remaining gaps beyond the two storage policies (re-read research.md Parts A/B fresh, since it's been many firings since a full re-read rather than section-targeted re-skims). No strong preference — (a) and (b) both round out existing, already-flagged gaps rather than opening new scope.
- **Open questions for the user:** none blocking. Same standing notes as recent firings (RISC-V/ARM/AVR toolchains not found on this machine).
- **Subagents launched this project so far:** 9 total (unchanged — documentation work, no fresh design question).
  1. C++ design/architecture research for a tiny event system → became research.md Part A.
  2. CMake/compiler-flags research for embedded zero-dependency builds → became research.md Part B.
  3. Phase 0 library header/API architecture proposal → adopted into ARCHITECTURE.md.
  4. Phase 0 CMake/test/tooling architecture proposal → adopted into ARCHITECTURE.md (one suggestion rejected).
  5. `delegate<Sig>` toolchain/GCC-version portability investigation → confirmed safe; flagged avr-gcc 7.3.0 as lowest-margin (still safe); flagged ICF folding isn't automatic on Lw-Eh's target toolchains without `-flto`/gold/lld.
  6. `delegate<Sig>` EnTT cross-validation / language-correctness investigation → confirmed the two-overload `bind` design; surfaced the `std::invoke` footgun now baked into the implementation.
  7. `signal<Event,N>` reentrancy: EnTT's actual `sigh`/`sink` source investigation → found EnTT's real removal mechanism is swap-and-pop (not shift-down), traced a confirmed double-invoke + UB-OOB hazard for detaching not-yet-visited peers, surveyed ETL/QP/Linux-kernel prior art for the mark-and-skip pattern.
  8. `signal<Event,N>` reentrancy: adversarial stress-test of the proposed mark-and-skip design → compiled+ASan/UBSan-verified every reentrancy scenario safe, established the exact attach-during-dispatch deferral contract now documented in `signal.hpp`.
  9. `intrusive_signal<Event>` reentrancy: adversarial stress-test of the proposed singly-linked head-insertion design → confirmed self-detach/detach-other/attach-during-dispatch all safe, but found and precisely characterized a real compound-reentrancy bug (self-detach + detach-own-frozen-successor), now documented as a known caveat rather than engineered around.

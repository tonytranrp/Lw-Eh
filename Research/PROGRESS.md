# Lw-Eh Progress Ledger

This is the durable, git-tracked ledger for the autonomous build loop. Updated at the end of every firing. Git history is ground truth if this ever drifts — see `git log --oneline`.

**Format note (firing 37):** `Last completed` holds only the most recent firing's summary, not a running narrative — every prior firing's full detail (what was found, why, what was verified, exact commands/output) is preserved verbatim in its own commit message(s), which is where cross-references and future digging should go. This entry had grown to four stacked multi-paragraph essays (firings 34-36) before this trim; nothing in them was lost, they're all still in `git log`.

- **Phase:** hardening (real-hardware-validation thread in progress, firing 38 — see below; not moving the phase field backward, the core library remains fully implemented/tested, this is new validation depth becoming available, not a lifecycle regression)
- **Last completed (firing 37):** Trimmed this file back to the one-entry `Last completed` format the ledger's own template has always specified, after it drifted to four full historical essays across firings 34-36. Documentation-only; no code/build/test/measurement involved.
- **In progress (firing 38):** The user confirmed a real ESP32 board is now connected to this machine for actual hardware validation — a first for this project (see commit `ee63b05` and durable memory for the full hardware confirmation: COM5, CP210x bridge, MAC `8c:94:df:93:b4:50`, esptool.py working with a flaky-but-functional auto-reset). `examples/esp32_minimal/` has only ever been used for static size measurement — its linker script and startup code are explicitly documented placeholders, never actually flashed or booted. Launched two parallel subagent investigations before touching any code.
  **Subagent #17 (image/flashing mechanics) returned — decisive, well-evidenced findings, no repo changes yet:**
  - **The load-bearing question, answered with high confidence via primary sources, not general knowledge**: does the prebuilt Arduino-ESP32 second-stage bootloader require an ESP-IDF-style app-descriptor header a from-scratch app can't easily produce? **No.** Confirmed four ways: the documented image-verify contract (magic byte/checksum/hash/signature only) has no descriptor requirement; the real ESP-IDF v4.4.7 source (fetched and read, matching this framework's exact version) gates descriptor-checking behind `CONFIG_BOOTLOADER_APP_ANTI_ROLLBACK`, off by default; running `xtensa-esp32-elf-nm` directly on the actual local `bootloader_qio_40m.elf` shows the descriptor-checking function isn't even linked into this specific prebuilt binary; and the actual jump sequence in `bootloader_utility.c` is a raw function-pointer call to the image header's entry address, no descriptor lookup. This was the single biggest open risk and it's resolved clean.
  - **Real landmine caught before it bit**: the prebuilt `tools/partitions/default.bin` does NOT match `tools/partitions/default.csv` in this exact framework version — verified by regenerating from the CSV and byte-diffing (differ at byte 132; the prebuilt has a stale `eeprom` partition where the current CSV has `spiffs`+`coredump`). **Must regenerate `partitions.bin` from the CSV via `gen_esp32part.py`, never use the prebuilt `default.bin` directly.**
  - `elf2image` mechanics confirmed empirically against the real `examples/esp32_minimal` ELF (built via `cmake --preset esp32-xtensa`): entry point is a raw copy of `e_entry` (no symbol lookup, `ENTRY(_start)` is sufficient); `.bss` sections are read via the ELF section table and **completely excluded from the output image** (routed to a `nobits_sections` list, never written) — direct confirmation that whatever currently lives in `.bss`-mapped RAM at boot is genuinely uninitialized garbage unless startup code zeroes it, not just a style nitpick; no address-range validation happens at `elf2image` time (succeeded silently converting the current, unmodified, IRAM-only-layout ELF) — real-address correctness is instead checked later, at flash-time, by the bootloader itself (`verify_load_addresses()`, which only rejects overlap with the *bootloader's own* reserved regions — a non-issue for an example this small). Do NOT pass `--elf-sha256-offset` (that's for ESP-IDF apps with a `.flash.appdesc` section reserved for it; passing it against a from-scratch ELF hits a real, confirmed "not all zero, refusing to overwrite" fatal error).
  - Bootloader variant: `dio`+`40m` is the correct safe default for an unbranded/generic board (confirmed via this exact machine's own `esp32dev.json` board definition AND PlatformIO's own qio→dio/qout→dio downgrade-for-unknown-boards logic).
  - Flash offsets verified against the real, current `default.csv` (not assumed): bootloader `0x1000`, partition table `0x8000`, `boot_app0.bin` `0xe000` (needed — the CSV does include an `otadata` partition), app image `0x10000`. Full working `write_flash` command recorded in commit `f4a09c6`.
  **Subagent #18 (startup-code/memory-layout correctness) returned — found a real, hardware-breaking bug, then built and verified a fix (see commit `e438462` for the full implementation).** Empirically confirmed via `nm` on the real linked ELF that `scenario.hpp`'s global object graph needs zero dynamic C++ initialization (no `_GLOBAL__sub_I_*`, no `.init_array`/`.ctors` content) — just `.bss` zeroing and a `.data` load-to-run copy. But proved (via a real disassembled instruction, not theory) that the old flat `0x40080000` single-region layout placed `.data`/`.bss` — including several `bool` globals — inside IRAM0, the word-only-accessible instruction bus: a byte-store inside `on_battery_level()` targeting an IRAM0 address, which would raise a `LoadStoreError` on real silicon. Built, compiled, and linked a fix (separate IRAM0/DRAM0 memory regions, real LMA≠VMA `.data` split, `.bss` zeroing, defensive `.ctors` walk) and confirmed via `readelf -l` + re-disassembly that it actually closes the bug. **This fix was independently re-verified directly against the real repo files (not just trusted from the subagent's scratch copy) and landed in commit `e438462`**: `cmake --build --preset esp32-xtensa` compiles/links clean, `readelf -l` shows the correct LMA≠VMA program headers, and the previously-faulting store now resolves to `0x3ffb00ad`, `g_battery_low`'s real DRAM0 address.
  **First real flash attempt: succeeded completely.** Converted the fixed ELF + the real, already-installed Arduino-ESP32 prebuilt `bootloader_dio_40m.elf` + a freshly-regenerated `partitions.bin` (not the stale prebuilt) + `boot_app0.bin` into a full image set and flashed all four components to the real board at the verified offsets — every segment written and hash-verified (`Chip is ESP32-D0WD-V3 (revision v3.1)`, `MAC: 8c:94:df:93:b4:50`). Separately confirmed via `flash_id`: 4MB flash detected (matches what was assumed), "flash voltage set by a strapping pin."
  **But the board doesn't actually boot into the app — a real, reproduced boot loop.** Captured real boot output over serial (pyserial, 115200 baud, confirmed reproducible byte-for-byte across two independent flash+observe cycles with zero manual DTR/RTS interference, so not a test-methodology artifact): the ROM bootloader successfully loads the prebuilt second-stage bootloader and jumps to its entry point (`0x400805e4`) — consistently, correctly — then total silence for many seconds, then `rst:0x10 (RTCWDT_RTC_RESET)` (an RTC watchdog firing) and the whole sequence repeats. The bootloader itself never produces a single byte of its own log output before the watchdog resets it, which is not normal — a real ESP-IDF/Arduino bootloader logs almost immediately. **This is very likely a problem in how the reused, prebuilt Espressif bootloader binary was converted/packaged, not in Lw-Eh's own code or the just-verified linker/startup fix** (which only affects `firmware.bin`, loaded later in the boot chain — the hang happens before that point is ever reached). Launched a third, tightly-scoped subagent investigation armed with the full captured boot log and flash-command history, with direct real-hardware access to test hypotheses (bootloader-specific packaging steps a generic `elf2image` conversion might be missing, chip-revision compatibility, flash-voltage-strapping interactions, or something else) — in flight as of this write-up.
  **Loose end resolved (firing 39):** by the time this thread reached `examples/esp32_minimal/CMakeLists.txt`, it was back to its original, unmodified content — nothing recoverable was overwritten (no git object ever existed for whatever the diagnosis subagent's edit contained, so there was nothing to diff against). Independently investigated the exact same question that edit was reportedly testing (does `-mabi=call0` remove windowed `entry`/`retw` prologues, closing the "does `load_ram` leave `PS.WOE`/window exception vectors configured" open question) and found a hard, verified answer: **no, this specific prebuilt toolchain has no call0 ABI support at all** — confirmed via `-print-multi-lib` (only `.;`, `esp32-psram;`, `no-rtti;`, `esp32-psram/no-rtti;`, no call0 variant) and `xtensa-esp32-elf-g++` rejects `-mabi=call0`/`--abi-call0` outright as unrecognized. Recorded as a permanent comment in `CMakeLists.txt` (commit `8f2f413`) so no future firing re-attempts this dead end. Does not explain the boot loop above (that hangs before `firmware.bin` ever runs), but is real signal for whenever windowed-ABI correctness on this boot path becomes load-bearing again — the fix then would have to be real vector-table code in `startup.S`, not a flag.
  **Strong, evidence-based root-cause hypothesis for the boot loop (firing 40) — NOT YET TESTED on real hardware, COM5 was actively held by another session throughout this investigation.** Dispatched a subagent to read the real ESP-IDF v4.4.7 source (version confirmed straight from the shipped `esp_idf_version.h`, not inferred) for `bootloader_init()`'s exact early-boot call sequence (`components/bootloader_support/src/esp32/bootloader_esp32.c`). Key finding: this exact prebuilt bootloader's own `sdkconfig` has `CONFIG_BOOTLOADER_LOG_LEVEL_NONE=y` — it is physically incapable of printing anything, success or failure, so the "silence" observed is expected regardless of where a hang occurs and isn't itself informative. The real signal is **timing**: the observed ~9-second delay before `RTCWDT_RTC_RESET` matches `CONFIG_BOOTLOADER_WDT_TIME_MS=9000`, the value the watchdog is *freshly re-armed to* at step 15 of `bootloader_init()` (`bootloader_config_wdt()`) — meaning steps 1-15 (including step 13's `bootloader_init_spi_flash()`, which configures the SPI peripheral's GPIO routing and dummy-cycle timing **from the bootloader image's own header mode/freq bytes**) all completed, and the actual hang happens later, most likely the first genuine flash read afterward (partition-table load) using a since-misconfigured SPI setup.
  Followed the subagent's recommended zero-risk-first experiment myself (pure local file inspection, no hardware/COM5 touched): converted the real local `bootloader_dio_40m.elf` via `elf2image` **without** an explicit `--flash_mode` flag (a plausible omission, since no literal flash command survived in git history to confirm either way) and read back its header via `image_info --version 2`. **Confirmed: it defaults to `Flash mode: QIO`** — esptool's own `elf2image` argument parser defaults `--flash_mode` to the literal string `"qio"` regardless of the ELF's own filename/intent. This is a real, hard mismatch against the board's DIO-only flash wiring (already an established fact in this project's ledger). **Strong corroboration, not just plausibility**: this default-flags image's entry point (`0x400805e4`) is byte-identical to the entry point address in the actual captured real-hardware boot log from the earlier flash attempt — strong circumstantial evidence this is genuinely the same misconfiguration that's on the board right now, not just a hypothetical.
  Regenerated the same ELF **with** explicit `--flash_mode dio --flash_freq 40m --flash_size 4MB` and confirmed via `image_info` + byte-diff that this produces a materially different image (header bytes at offset 3/4 change from `Flash mode: QIO`/`Flash size: 1MB` to `Flash mode: DIO`/`Flash size: 4MB`, different trailing checksum/hash as expected). **This is now a concrete, ready-to-test fix**: reflash only the bootloader at offset `0x1000` with this corrected image, power-cycle, and check whether the boot-loop symptom changes — the next real-hardware step for whichever thread gets COM5 access next. Both scratch images are local temp files (`elf2image` is fully deterministic and reproducible from the same source ELF + these exact flags — no need to hunt for a specific saved file to redo this check).
- **Firing 39 (this thread) — three further increments, all committed and pushed, none touching the physical board/COM5** (a separate concurrent investigation had an active real-hardware boot-loop diagnosis in progress there — see above):
  1. Resolved the CMakeLists.txt loose end (commit `8f2f413`, described above).
  2. **Fixed a real size-measurement methodology bug** (commit `e34f3ad`): `size_audit`'s control baseline (`control_main.cpp`) still linked via the toolchain's generic newlib crt0, while the with-lweh side (`lw_eh_example_esp32_minimal`) had just gained a completely different, custom, from-scratch boot path (`e438462`) — violating research.md B9's "otherwise identical builds" requirement and silently contaminating the "incremental" number with boot-path differences that have nothing to do with Lw-Eh. Added `lw_eh_size_audit_control_esp32`, sharing the exact same `startup.S`/`esp32.ld` as the real example. Needed its own `enable_language(ASM)` call — confirmed the hard way that `examples/CMakeLists.txt`'s own call is scoped to its own directory subtree (a sibling of `size_audit/` under the root `CMakeLists.txt`, not an ancestor) and never propagated; a fresh configure without this addition failed with "required internal CMake variable not set: `CMAKE_ASM_COMPILE_OBJECT`". Corrected measurement: control_esp32 is only 97 bytes (the from-scratch startup path is genuinely lighter than the toolchain's generic crt0, not heavier) — true incremental cost is 822 bytes, not the 564 recorded a firing ago.
  3. **Added minimal bare-metal UART0 TX logging** (commit `0feb382`), closing the exact gap firing 38 flagged ("even a successful boot can't yet be distinguished from a silent hang"). `app_main()` now prints `LWEH BOOT\r\n` on entry and `LWEH OK\r\n` after `run_scenario()` completes. Register map (base `0x3ff40000`, FIFO at `+0x0`, status/`TXFIFO_CNT` at `+0x1C` bits `[23:16]`, `CLKDIV` at `+0x14`) and POR-state claims (UART0 already clocked, pin-routed, and running ~115200 8N1 from power-on, no init required on this boot path) came from a dedicated subagent investigation reading the real local Arduino-ESP32 framework headers directly and cross-checking via `nm` against the real prebuilt bootloader ELF — not general knowledge, per the standing rule this same firing's call0 dead-end established. Verified via objdump + literal-pool decoding against the actual built ELF that the generated code loads exactly those three addresses. `size_audit/control_esp32_main.cpp` calls the identical logging so the measurement isolation from item 2 stays intact (824 bytes final, the 2-byte drift from 822 is layout noise, not a real cost). `examples/esp32_minimal/uart.hpp` is example-only scaffolding, deliberately not under `include/lweh/`.
- **Firing 41: research.md documentation fix, no code/build/test involved (small change).** COM5 remained held by the concurrent boot-loop-diagnosis thread throughout (confirmed genuinely active via repeated port-open probes and `Get-Process` showing continuously-spawning new python processes, not a stale handle) — re-checked for other independent work rather than force hardware access, and found research.md Part A §11 ("Open decisions to resolve before writing code") had gone stale: a pre-implementation planning note from before any code existed, never updated as those decisions actually got made across ~40 firings of real implementation. All three were in fact long since resolved — both listener-storage policies were built (not one chosen over the other), C++17 was pinned in the root `CMakeLists.txt`, and ISR-safe queued dispatch was deliberately declined (not deferred), already documented in `signal.hpp`/`intrusive_core.hpp`'s own dispatch-safety contracts citing this same research file's Boost.Signals2 lesson. Rewrote it as a resolved-decisions record with exact pointers, so a future firing re-skimming this file (as the loop's own orientation step requires) doesn't mistake settled design decisions for open ones. Commit `ad3a29f`.
- **Current measured size:** **824 bytes incremental** for 6 event types on real ESP32 Xtensa hardware (`xtensa-esp32-elf-gcc` 8.4.0, `-Os -flto -flto-partition=none` + full aggressive flag set, measured 2026-07-16 via `size_audit/size_report.py --control-target lw_eh_size_audit_control_esp32 --with-target lw_eh_example_esp32_minimal`; control_esp32 203 bytes, with-lweh 1027 bytes). This number has moved twice in one day (471 → 564 → 824) — none of it is Lw-Eh's own code growing; every jump was a measurement-methodology correction (first: `.bss`/`.data`/`.ctors` startup cost that a broken placeholder was skipping; second, firing 39: fixing the control baseline's boot path to genuinely match the with-lweh side, per research.md B9 — see above). The former "known measurement-precision caveat" is resolved; 824 bytes is comfortably within the 1-2KB budget for 6 event types per research.md A10.
- **Test status:** 7/7 pass under `host-debug` (last full run 2026-07-16, commit `01d7012` — unaffected by this firing's changes, which are scoped entirely to `examples/esp32_minimal/`, never touched by any test).
- **Firing 42: firing 40's leading hypothesis directly tested on real hardware and definitively ruled out.** Held COM5 for an extended real-hardware session (the concurrent thread's own firing-41 entry confirms it saw COM5 as busy throughout — that was this thread). Before reflashing, verified this project's own `bootloader.bin` (built by this same thread's earlier firing-38 conversion, artifacts still present in scratch) via `image_info --version 2`: header already reads `Flash mode: DIO`, `Flash freq: 40m`, `Flash size: 4MB` — the exact values firing 40's proposed fix calls for. **The flash-mode-mismatch hypothesis, while a real and correctly-identified risk in general (`elf2image` genuinely does default to QIO if `--flash_mode` is omitted, confirmed by that investigation), does not apply to this project's actual artifacts — they were never misconfigured that way.** Confirmed decisively rather than argued from the header alone: erased the chip's entire flash (`erase_flash`, breaking any possible stale-image ambiguity), reflashed the complete image set (bootloader + partitions + boot_app0 + firmware, all with explicit `--flash_mode dio --flash_freq 40m --flash_size 4MB`) fresh, and captured boot output. **Identical symptom, byte-for-byte**: `rst:0x10 (RTCWDT_RTC_RESET)`, same `entry 0x400805e4`, same silence, same loop. A genuinely correct-header bootloader hangs exactly the same way a hypothetically-wrong-header one would have — this rules the hypothesis out with real evidence, not just a header inspection.
  Two secondary findings from the same session: (1) **eFuse check** (`espefuse.py summary`): `XPD_SDIO_FORCE=False`, meaning VDD_SDIO flash voltage is determined live by the GPIO12 strap at each reset, not fixed by a bad eFuse write — rules out a hard, permanent voltage misconfiguration as the cause (a live GPIO12 strapping issue can't be ruled out this way, but eFuse-level force is definitively not it, and eFuse writes are irreversible so this was never going to be "fixed" here regardless). (2) **`esptool.py load_ram` successfully loaded Lw-Eh's own fixed `firmware.bin` directly into RAM and began executing it at its real entry point (`0x40080284`), completely bypassing the reused bootloader chain** — real, positive evidence that the ROM's download-mode monitor accepts this project's own image format without complaint and that firing 38's IRAM0/DRAM0 fix produces something the chip is willing to start running. Could NOT conclusively confirm the RAM-loaded firmware ran to completion or printed `LWEH BOOT`/`LWEH OK` correctly — every capture attempt raced against either an accidental reset from opening a fresh serial connection, or (once that was fixed via pre-set `dtr=False`/`rts=False` before `.open()`) the firmware's own sub-5ms runtime completing before the capture connection could open at all. Built a scratch-only repeating diagnostic firmware to work around the timing race but couldn't get `load_ram` to succeed reliably enough in the time available to run it (auto-reset success rate degraded sharply during this session — dozens of consecutive failures where earlier firings saw roughly 1-in-3 to 1-in-5; erasing flash improved but did not fully fix this). This remains a genuinely open, unresolved question — not a negative result, an incomplete one.
  **Chip state as of this writing**: flashed with the same image set tested throughout this thread (verified-correct headers), actively boot-looping. Scratch artifacts (`bootloader.bin`, `partitions.bin`, `boot_app0.bin`, `firmware.bin`, plus the diagnostic `diag.elf`/`diag.bin`) are in this session's scratchpad, not the repo — reproducible from source via the recipe in commit `f4a09c6` if needed again.
- **Next up:** The flash-mode hypothesis is closed. The next candidate, per firing 40's own ranked list, is a hang inside `bootloader_utility_load_boot_image()`'s partition/app-image read path — worth a fresh, targeted investigation (real ESP-IDF source read, ideally cross-checked against the real prebuilt bootloader binary's own linked code the way firing 38's app-descriptor question was). Separately, the `load_ram` timing-race problem (can real firmware output be captured reliably without a bootloader in the loop at all) is its own smaller, well-scoped follow-on worth solving on its own merits — it would give this project a hardware-validation path that doesn't depend on fixing the reused bootloader at all. Both are legitimate next threads; whichever gets picked up first should re-check this file and git log fresh before starting, per the standing concurrent-firings guidance in durable memory. Standing candidate, unchanged, lower priority: RISC-V/ARM/AVR toolchains remain the largest unresolved gap for those three architectures specifically, needs explicit user permission to install.
- **Open questions for the user:** none blocking. Standing notes carried forward: the machine's global `http.postbuffer=500MB` git setting can still cause push OOM errors under concurrent memory pressure (workaround: per-command `-c http.postbuffer=` override, documented in durable memory, never edit the persisted config); RISC-V/ARM/AVR toolchains not present on this machine, installing one is the user's call.
- **Subagents launched this project so far:** 21 total (item 19, firing 38's boot-loop diagnosis, may still be in flight in a separate concurrent thread — status unknown to this thread as of this writing).
  1. C++ design/architecture research for a tiny event system → became research.md Part A.
  2. CMake/compiler-flags research for embedded zero-dependency builds → became research.md Part B.
  3. Phase 0 library header/API architecture proposal → adopted into ARCHITECTURE.md.
  4. Phase 0 CMake/test/tooling architecture proposal → adopted into ARCHITECTURE.md (one suggestion rejected).
  5. `delegate<Sig>` toolchain/GCC-version portability investigation → confirmed safe; flagged avr-gcc 7.3.0 as lowest-margin (still safe); flagged ICF folding isn't automatic on Lw-Eh's target toolchains without `-flto`/gold/lld.
  6. `delegate<Sig>` EnTT cross-validation / language-correctness investigation → confirmed the two-overload `bind` design; surfaced the `std::invoke` footgun now baked into the implementation.
  7. `signal<Event,N>` reentrancy: EnTT's actual `sigh`/`sink` source investigation → found EnTT's real removal mechanism is swap-and-pop (not shift-down), traced a confirmed double-invoke + UB-OOB hazard for detaching not-yet-visited peers, surveyed ETL/QP/Linux-kernel prior art for the mark-and-skip pattern.
  8. `signal<Event,N>` reentrancy: adversarial stress-test of the proposed mark-and-skip design → compiled+ASan/UBSan-verified every reentrancy scenario safe, established the exact attach-during-dispatch deferral contract now documented in `signal.hpp`; kept permanently standing via the `host-asan` preset since firing 21, and via CI since firing 28.
  9. `intrusive_signal<Event>` reentrancy: adversarial stress-test of the proposed singly-linked head-insertion design → confirmed self-detach/detach-other/attach-during-dispatch all safe, but found and precisely characterized a real compound-reentrancy bug (self-detach + detach-own-frozen-successor), now documented as a known caveat rather than engineered around.
  10. `signal_core` deferral pressure-test (firing 19): given real firing-16/17/18 measured data, does the Phase-0 amendment's own revisit trigger apply now? → no, budget headroom is comfortable; recommended deferring the full rewrite while flagging `attach()`'s uncompensated duplication as real.
  11. Narrower `attach()`/`detach()`-only shared-core design investigation (firing 19): can the Event-agnostic slot-scan be factored out without the full event-erasure adapter-stub cost the original design needed? → yes, a `slot_view`-based runtime-count finder is technically sound (with AVR Harvard-architecture and strict-aliasing caveats addressed); also identified the per-(Event,MaxListeners,Fn) duplication axis; proposed the noinline experiment as a cheaper first test, which the loop then ran directly and found to regress size by 62 bytes, decisively ruling it out.
  12. `requires`-clause technical correctness investigation (firing 23): exact syntax, C++17 fallback exactness, binary-size impact, and interaction with the existing `is_member_pointer_v` static_assert → syntax confirmed correct and verified empirically against real clang compilation, but found `is_invocable_r_v` has its own INVOKE-based loophole matching the file's own documented `std::invoke`-avoidance rationale, and that a `requires`-clause failure pre-empts/hides the friendlier body `static_assert` message via SFINAE — directly shaped the final implementation.
  13. Concept-constraint value/minimalism evaluation (firing 23): is this worth doing at all, benchmarked explicitly against firing 19's `signal_core` deferral precedent → no, recommended a plain C++17 `static_assert(is_invocable_r_v<...>)` instead of `requires`, and removing `LWEH_HAS_CONCEPTS` as dead cruft with no other plausible future use found.
  14. Compile-fail testing mechanics investigation (firing 24): what's the correct, robust way to test "this snippet must fail to compile with this message" on this exact CMake/Ninja/Clang/Windows setup → found `try_compile()` has a real, empirically-confirmed header-dependency-tracking gap that would have silently broken the whole mechanism; delivered a complete, tested, ready-to-use alternative (direct compiler invocation via a `cmake -P` driver), not used to build permanent infrastructure but its verified compiler-invocation recipe was reused directly for that firing's one-time manual verification.
  15. Compile-fail testing value evaluation (firing 24): is permanent CTest infrastructure justified for 3 narrow, zero-incidence checks → no, benchmarked against both the `host-asan` precedent (worth building) and the `signal_core` precedent (correctly deferred) and landed closer to the latter; recommended a one-time, dated, recorded verification instead, which the loop then performed for real.
  16. GCC 8.4.0 IPA-ICF vtable-folding-asymmetry investigation (firing 34): given real disassembly showing byte-identical `on_event()` overrides folded to one address while byte-identical `invoke_erased()` overrides did not, in the same binary — why the difference? → built a faithful repro on the real toolchain with `-fdump-ipa-icf-details`, confirmed empirically (via the dump's `"Equals called for:"` log lines, not guessed): ICF hash-buckets candidates before ever comparing bodies, `on_event`'s instantiations hash identically and fold, `invoke_erased`'s hash differently and never reach body comparison, because its call to `on_event` is polymorphic/indirect (hashed more richly, type-leaking) vs. `publish()`'s direct call to `dispatch()` (hashed thin, folds fine) — a clean internal control group ruled out simpler theories. Not fixable without collapsing the erasure-thunk into the listener's own virtual method (a real architecture tradeoff for ~tens of bytes); no code change made, matches pre-registered scoping.
  17. ESP32 image/flashing mechanics investigation (firing 38): given real hardware + esptool.py + prebuilt Arduino-ESP32 bootloader/partition files but no ESP-IDF/pio CLI, what's the exact `elf2image`/`write_flash` recipe to produce a genuinely bootable image, and critically whether the second-stage bootloader hard-requires an ESP-IDF-style app-descriptor header a from-scratch app wouldn't have → no descriptor required (confirmed via `nm` on the real local bootloader ELF, the checking code isn't even linked in); full verified recipe + a real landmine caught (stale prebuilt `default.bin`) delivered, see commit `f4a09c6`.
  18. Real-hardware startup-code correctness investigation (firing 38): does `scenario.hpp`'s global `signal<>`/`intrusive_signal<>` object graph actually need `.bss` zeroing/`.data` copy-down/C++ static-init to behave correctly on real silicon (not just "is it bad practice"), and is the current linker script's flat IRAM-only memory layout structurally viable for real execution → zero dynamic init needed (confirmed via `nm`), but the flat layout is genuinely broken (a real byte-store to IRAM0 confirmed via disassembly, would fault on real silicon); built, linked, and verified a fix (real IRAM0/DRAM0 split, LMA≠VMA `.data`). Implemented and independently re-verified, landed in commit `e438462`.
  19. Real-hardware bootloader boot-loop diagnosis (firing 38): the flash succeeds completely (all 4 components hash-verified) and the ROM correctly loads the prebuilt second-stage bootloader, but it hangs silently before producing any log output and the RTC watchdog resets it in a loop — why? → in flight, result pending; has direct access to the real hardware to test hypotheses, not just reason abstractly.
  20. ESP32 bare-metal UART0 TX register investigation (firing 39): exact base address/FIFO/status-register layout for polled UART0 TX, and whether clock/GPIO-matrix/baud pre-init is required from a cold from-scratch entry point (no SDK) → base `0x3ff40000`, FIFO at `+0x0`, status/`TXFIFO_CNT` at `+0x1C` bits `[23:16]`, `CLKDIV` at `+0x14` (all cited to real local `soc/*_reg.h` headers, cross-checked via `nm` against the real bootloader ELF's own `UART0` absolute symbol — exact match); no pre-init needed, UART0 is clocked/pin-routed/~115200-8N1 from power-on reset on this specific boot path (ROM + bootloader already rely on exactly this). Implemented directly, verified via objdump/literal-pool decoding against the real built ELF.
  21. ESP32 bootloader silent-hang root-cause research (firing 40): given the real captured boot log (loads, jumps to entry, ~9s silence, RTC watchdog reset, repeats), what does real ESP-IDF v4.4.7 bootloader source do between entry and its first log line, and what's the most likely from-scratch-flashing-specific cause → found this exact prebuilt bootloader's logging is entirely compiled out (`CONFIG_BOOTLOADER_LOG_LEVEL_NONE=y`), so silence alone was never diagnostic; the ~9s timing matches the watchdog's fresh 9000ms re-arm partway through `bootloader_init()`, meaning the hang is later than a naive reading suggested; ranked SPI flash-mode mismatch (image header consumed at the flash-init step) as the leading cause. Independently reproduced and confirmed locally (no hardware touched): `elf2image` on `bootloader_dio_40m.elf` without an explicit `--flash_mode` defaults to `QIO` against DIO-only board wiring, with an entry-point address matching the real captured boot log exactly; regenerating with explicit `--flash_mode dio --flash_freq 40m --flash_size 4MB` produces a verified, materially different, ready-to-test image. Not yet tried on real hardware — COM5 was actively held by a separate concurrent session throughout.

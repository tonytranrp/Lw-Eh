# Lw-Eh Progress Ledger

This is the durable, git-tracked ledger for the autonomous build loop. Updated at the end of every firing. Git history is ground truth if this ever drifts — see `git log --oneline`.

- **Phase:** hardening (unchanged)
- **Last completed:** Firing 28 fully resolved the CI push saga (workflow live, execution confirmed green, root cause of the push blocker diagnosed). Fresh orientation this firing noticed a genuine, real gap that closing the CI loop made newly practical to fix: the host correctness suite has run under Clang (`host-debug`/`host-asan`) across every one of the last 29 firings, but **never once under real GNU GCC** — and this project's own dev machine has no way to test that locally at all (`g++ --version` on this machine prints `clang version 22.1.4`; the llvm-mingw toolchain's `g++` is a Clang alias, not genuine GNU GCC, matching `Research/ARCHITECTURE.md`'s own environment note). This isn't a cosmetic gap: research.md §B7 frames GCC, not Clang, as the *real production toolchain family* for the actual embedded deployment target (`xtensa-esp32-elf-gcc`), and `cmake/LwEhAggressiveFlags.cmake` already has GNU-specific branches (`-Os` vs. Clang's `-Oz`, `-flto -flto-partition=none`, `-Wl,--relax` deliberately scoped to GNU only per firing 15's own investigation) that had been reasoned about and written correctly against research.md, but never actually *compiled* with a genuine GNU compiler on any target, host or embedded, across the whole project.
  Added a `host-gcc` CMake preset mirroring `host-debug`'s shape (`CMAKE_CXX_COMPILER=g++`/`CMAKE_C_COMPILER=gcc`) and extended CI's `host-tests` matrix to include it. Verified structurally on the local dev machine first (preset configures cleanly, JSON valid, 7/7 tests pass) with the explicit, documented caveat that the local run still exercises Clang under the hood via the `g++` alias and can't verify the real GNU-specific flag paths at all — that verification could only happen in CI. Pushed (first-try success using the `http.postbuffer` override discovered firing 28 — the memory-pressure issue itself remains a standing environmental fact, not something this firing needed to re-diagnose), then used `gh run watch`/`gh run view --json` to confirm the real outcome rather than assume it: **all four CI jobs passed** (`Hermeticity lint`, `Host tests (host-debug)`, `Host tests (host-asan)`, `Host tests (host-gcc)` — all `success`, overall `completed`/`success`). Pulled the exact toolchain version from the CI logs for the record: `g++ (Ubuntu 13.3.0-6ubuntu2~24.04.1) 13.3.0`, genuine GNU GCC, Ubuntu 24.04 — **the library's core correctness suite, and every GNU-specific branch of the aggressive compiler/linker flag set, has now been empirically confirmed to actually work under real GCC for the first time in this project's history**, not just reasoned about from documentation.
- **Current measured size:** Unchanged — **375 bytes incremental** for 5 event types on real ESP32 Xtensa hardware (firing 23, last time anything compiled for the embedded target changed). This firing added host-only test coverage; no library or embedded-target code changed.
- **Test status:** 7/7 pass under `host-debug`, `host-asan`, **and now `host-gcc`** — all three confirmed both locally (structural only, for host-gcc) and for real in CI (all three fully meaningful in CI, since CI has genuine compilers for all of them). This is the widest the correctness suite has ever been verified: two independent compiler families (Clang 18.1.3 and GCC 13.3.0 on CI's Ubuntu 24.04 runner, plus Clang 22.1.4 locally), one with sanitizers.
- **Next up:** open — this was a self-contained, fully-verified unit of work. Reasonable follow-on candidates for a future firing, not started now: a `host-gcc`+sanitizer combination (GCC also supports `-fsanitize=address,undefined`; not pursued this firing since the primary gap — "has GNU GCC ever compiled this at all" — is now closed, and the marginal value of also sanitizing under GCC specifically is lower than what this firing closed). Standing candidate, unchanged and lower priority: RISC-V/ARM/AVR toolchains remain the largest unresolved gap, needs explicit user permission to install and isn't being sought since it isn't blocking anything above.
- **Open questions for the user:** none blocking. Standing notes carried forward: the machine's global `http.postbuffer=500MB` git setting can still cause push OOM errors under concurrent memory pressure (workaround documented in durable memory, `project_lw-eh-git-push-oom-workaround.md` — not re-triggered this firing, first push attempt succeeded outright); RISC-V/ARM/AVR toolchains not present on this machine, installing one is the user's call.
- **Subagents launched this project so far:** 15 total (unchanged — this firing was direct CMake/CI implementation and live verification, no fresh design question needed a subagent; the GCC-vs-Clang question itself was already investigated by subagent #5 back in Phase 0, this firing just closed the empirical-verification gap that investigation flagged but couldn't itself resolve).
  1. C++ design/architecture research for a tiny event system → became research.md Part A.
  2. CMake/compiler-flags research for embedded zero-dependency builds → became research.md Part B.
  3. Phase 0 library header/API architecture proposal → adopted into ARCHITECTURE.md.
  4. Phase 0 CMake/test/tooling architecture proposal → adopted into ARCHITECTURE.md (one suggestion rejected).
  5. `delegate<Sig>` toolchain/GCC-version portability investigation → confirmed safe; flagged avr-gcc 7.3.0 as lowest-margin (still safe); flagged ICF folding isn't automatic on Lw-Eh's target toolchains without `-flto`/gold/lld. **This firing closes the empirical-verification loop that investigation's own GCC concerns opened — real GCC 13.3.0 now confirmed compiling and passing everything.**
  6. `delegate<Sig>` EnTT cross-validation / language-correctness investigation → confirmed the two-overload `bind` design; surfaced the `std::invoke` footgun now baked into the implementation.
  7. `signal<Event,N>` reentrancy: EnTT's actual `sigh`/`sink` source investigation → found EnTT's real removal mechanism is swap-and-pop (not shift-down), traced a confirmed double-invoke + UB-OOB hazard for detaching not-yet-visited peers, surveyed ETL/QP/Linux-kernel prior art for the mark-and-skip pattern.
  8. `signal<Event,N>` reentrancy: adversarial stress-test of the proposed mark-and-skip design → compiled+ASan/UBSan-verified every reentrancy scenario safe, established the exact attach-during-dispatch deferral contract now documented in `signal.hpp`; kept permanently standing via the `host-asan` preset since firing 21, and via CI since firing 28 -- now also confirmed correct under GCC's own template/SFINAE resolution, not just Clang's.
  9. `intrusive_signal<Event>` reentrancy: adversarial stress-test of the proposed singly-linked head-insertion design → confirmed self-detach/detach-other/attach-during-dispatch all safe, but found and precisely characterized a real compound-reentrancy bug (self-detach + detach-own-frozen-successor), now documented as a known caveat rather than engineered around.
  10. `signal_core` deferral pressure-test (firing 19): given real firing-16/17/18 measured data, does the Phase-0 amendment's own revisit trigger apply now? → no, budget headroom is comfortable; recommended deferring the full rewrite while flagging `attach()`'s uncompensated duplication as real.
  11. Narrower `attach()`/`detach()`-only shared-core design investigation (firing 19): can the Event-agnostic slot-scan be factored out without the full event-erasure adapter-stub cost the original design needed? → yes, a `slot_view`-based runtime-count finder is technically sound (with AVR Harvard-architecture and strict-aliasing caveats addressed); also identified the per-(Event,MaxListeners,Fn) duplication axis; proposed the noinline experiment as a cheaper first test, which the loop then ran directly and found to regress size by 62 bytes, decisively ruling it out.
  12. `requires`-clause technical correctness investigation (firing 23): exact syntax, C++17 fallback exactness, binary-size impact, and interaction with the existing `is_member_pointer_v` static_assert → syntax confirmed correct and verified empirically against real clang compilation, but found `is_invocable_r_v` has its own INVOKE-based loophole matching the file's own documented `std::invoke`-avoidance rationale, and that a `requires`-clause failure pre-empts/hides the friendlier body `static_assert` message via SFINAE — directly shaped the final implementation.
  13. Concept-constraint value/minimalism evaluation (firing 23): is this worth doing at all, benchmarked explicitly against firing 19's `signal_core` deferral precedent → no, recommended a plain C++17 `static_assert(is_invocable_r_v<...>)` instead of `requires`, and removing `LWEH_HAS_CONCEPTS` as dead cruft with no other plausible future use found.
  14. Compile-fail testing mechanics investigation (firing 24): what's the correct, robust way to test "this snippet must fail to compile with this message" on this exact CMake/Ninja/Clang/Windows setup → found `try_compile()` has a real, empirically-confirmed header-dependency-tracking gap that would have silently broken the whole mechanism; delivered a complete, tested, ready-to-use alternative (direct compiler invocation via a `cmake -P` driver), not used to build permanent infrastructure but its verified compiler-invocation recipe was reused directly for that firing's one-time manual verification.
  15. Compile-fail testing value evaluation (firing 24): is permanent CTest infrastructure justified for 3 narrow, zero-incidence checks → no, benchmarked against both the `host-asan` precedent (worth building) and the `signal_core` precedent (correctly deferred) and landed closer to the latter; recommended a one-time, dated, recorded verification instead, which the loop then performed for real.

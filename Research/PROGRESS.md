# Lw-Eh Progress Ledger

This is the durable, git-tracked ledger for the autonomous build loop. Updated at the end of every firing. Git history is ground truth if this ever drifts — see `git log --oneline`.

- **Phase:** skeleton (Phase 0 complete)
- **Last completed:** Scaffolded the full architecture skeleton — synthesized two independent subagent proposals into [ARCHITECTURE.md](ARCHITECTURE.md), created `include/lweh/` stub headers, root `CMakeLists.txt` + `CMakePresets.json`, `tests/` with 5 placeholder CTest cases, `examples/` for all 4 embedded target families, `size_audit/` machinery, and `integrations/esp-idf/`. Verified the `host-debug` preset (Clang 22.1.4 + Ninja, pinned explicitly after CMake defaulted to MSVC) configures, builds clean under the real aggressive flag set (`-Oz -fno-exceptions -fno-rtti -flto=full -ffunction-sections -fdata-sections ...`), and all 5 placeholder tests pass via `ctest --preset host-debug`. Hermeticity grep gate (no `find_package`/`FetchContent`/`ExternalProject_Add` anywhere) is clean. Committed as `18a0d5c` and pushed to `origin/main`.
- **Current measured size:** N/A yet — no real implementation exists (every class in `include/lweh/` is an empty stub with only TODOs). Size measurement becomes meaningful starting Phase 1 once `delegate.hpp` has a real body. See environment note below re: embedded size measurement specifically.
- **Test status:** 5/5 placeholder tests pass (`ctest --preset host-debug`, full suite, 2026-07-16) — these only assert the stub types default-construct; none exercise real behavior yet. Judged as a "big" verification pass (first build of the whole skeleton) — full suite + full configure/build run was appropriate, not a targeted subset.
- **Next up:** Phase 1 — implement `lweh::delegate<Sig>` (`include/lweh/delegate.hpp`): the Ryazanov-style non-owning 2-pointer bind/invoke type (research.md §A1). Everything else (`signal_core`, `signal`, `intrusive_core`, `intrusive_signal`) depends on this existing first, so it's the correct next unit of work. Before writing it, per the loop's "investigate before implementing" rule, a targeted subagent question is warranted on the exact ABI-safe shape of the templated static-stub-pointer mechanism (member-function pointer binding specifically has real portability traps across compilers) — don't guess on this, it directly determines the delegate's binary layout.
- **Open questions for the user:** none blocking. Recorded as a non-blocking note: this dev machine has no embedded cross-compiler toolchains installed (`xtensa-esp32-elf-gcc`, `riscv32-esp-elf-gcc`, `arm-none-eabi-gcc`, `avr-gcc` are all absent — confirmed via `where.exe`), so the `examples/*` cross-compiles and real ESP32/ARM/AVR size measurement are blocked until one is installed. Not installing one autonomously (large download, PATH/environment modification) without the user's say-so. Host builds (Clang, `host-debug`/`size-host` presets) remain fully available as the interim proxy signal per research.md §B9. If the user wants real embedded validation sooner, installing e.g. the ESP-IDF toolchain would unblock it.
- **Subagents launched this project so far:** 4 total.
  1. C++ design/architecture research for a tiny event system → became research.md Part A.
  2. CMake/compiler-flags research for embedded zero-dependency builds → became research.md Part B.
  3. Phase 0 library header/API architecture proposal → adopted into ARCHITECTURE.md (namespace `lweh`, multi-header, `attach`/`detach`/`publish` vocabulary, static-array default storage).
  4. Phase 0 CMake/test/tooling architecture proposal → adopted into ARCHITECTURE.md (CMake target `lw_eh` kept in its own naming domain, hand-rolled test harness decision, `CMakePresets.json` structure, size-audit decoupled from CTest) — one incidental suggestion (`dispatcher.hpp`) explicitly rejected as conflicting with research.md §A2's settled "no runtime type registry" decision.

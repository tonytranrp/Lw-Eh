# Lw-Eh Progress Ledger

This is the durable, git-tracked ledger for the autonomous build loop. Updated at the end of every firing. Git history is ground truth if this ever drifts — see `git log --oneline`.

- **Phase:** embedded-validation (unchanged — this firing was host-only test coverage, no library/build code)
- **Last completed:** Started from last firing's "next up" (demonstrate a non-default `MaxListeners` capacity) but caught that the claim behind it was stale before doing redundant work: re-read `tests/capacity_boundary_test.cpp` directly and confirmed it's used `lweh::signal<dummy_event, 2>` (not the default 4) since firing 3 — the "still only demonstrated at its default" note, first written in firing 10's PROGRESS.md and repeated uncorrected through firing 11, was simply wrong. Recorded the correction here rather than silently dropping it. Pivoted to a genuine gap found by checking, not assuming: `lweh::delegate<Sig>::operator==`/`operator!=` (added in firing 5 specifically to support `signal<>`'s `detach<Fn>()`/`detach<MemFn>(T*)` value-matching, and documented with a real ICF-folding caveat) had **zero direct test coverage** — every existing test only exercised it indirectly, through whether `signal<>`'s attach/detach tests happened to pass. Added 5 direct test cases to `tests/delegate_test.cpp`: same free function bound separately compares equal; different free functions (same signature) compare unequal; same member function on the same instance compares equal; same member function on different instances compares unequal (`obj_` differs); a free-function-bound delegate vs. a member-function-bound delegate compare unequal; two default-constructed (unbound) delegates compare equal to each other (confirmed as the correct, consistent "nothing equals nothing" outcome, not a bug). Needed two new small file-scope helper free functions (`double_it`, `ignore_int`) to have genuinely distinct, correctly-signatured targets to compare against. **Honest note on how this firing actually went**: my first draft of the "different free functions" and "free vs. member" test blocks was badly broken — I referenced a nonexistent `math::scale_free_stand_in` method I'd invented without checking it existed, and wrote a nonsensical no-op assertion (`d_add != d_add == false ? true : true`) as a placeholder I then forgot to replace with a real check. Clangd's live diagnostics caught the compile error immediately; I read the file back fully, recognized both blocks were wrong (not just the flagged line), and rewrote them cleanly with the two new real helper functions rather than patching around the mistake. Recording this plainly rather than glossing over it, matching how earlier firings (e.g. firing 5's reentrancy test, firing 3's arithmetic slip) have handled catching their own errors — the catch-and-fix is the normal, expected part of the process, not something to hide.
- **Current measured size:** Unchanged — still **520 bytes incremental** on real ESP32 Xtensa hardware (firing 8/10/11). This firing added test-only code (never compiled into the library or any measured artifact), so no re-measurement was needed or taken.
- **Test status:** 7/7 pass (`ctest --preset host-debug`, 2026-07-16), full suite run. Judged as worth the full suite despite being a test-only, non-structural change (per the loop's rules this could have been scoped to just the `delegate` test) specifically because `delegate<Sig>` is the one type every other type in the library depends on — the marginal cost of running all 7 instead of 1 is under a second, cheap insurance for the foundational type.
- **Next up:** open choice: (a) the research.md re-read two firings ago found no remaining unaddressed gaps in the design itself, and this firing's "check before trusting a stale claim" approach paid off — worth applying the same check to the rest of PROGRESS.md's outstanding "next up" list before acting on any of it further (e.g. re-verify the `-flto-partition=none` comparison idea from firing 10 is still actually undone, rather than assuming); (b) `intrusive_signal`/`intrusive_node`'s own equality/comparison semantics (if any exist — worth checking whether they even have `operator==`, unlike `delegate`) could use the same direct-coverage audit just applied to `delegate`; (c) RISC-V/ARM/AVR toolchains remain the standing, largest unresolved gap, still blocked on toolchain availability this loop can't unilaterally fix. No strong preference; (a) is the most process-improving given what this firing just demonstrated.
- **Open questions for the user:** none blocking. Same standing notes as recent firings (RISC-V/ARM/AVR toolchains not found on this machine; `bloaty` confirmed not present).
- **Subagents launched this project so far:** 9 total (unchanged — this firing was direct test-writing work, no fresh design question).
  1. C++ design/architecture research for a tiny event system → became research.md Part A.
  2. CMake/compiler-flags research for embedded zero-dependency builds → became research.md Part B.
  3. Phase 0 library header/API architecture proposal → adopted into ARCHITECTURE.md.
  4. Phase 0 CMake/test/tooling architecture proposal → adopted into ARCHITECTURE.md (one suggestion rejected).
  5. `delegate<Sig>` toolchain/GCC-version portability investigation → confirmed safe; flagged avr-gcc 7.3.0 as lowest-margin (still safe); flagged ICF folding isn't automatic on Lw-Eh's target toolchains without `-flto`/gold/lld.
  6. `delegate<Sig>` EnTT cross-validation / language-correctness investigation → confirmed the two-overload `bind` design; surfaced the `std::invoke` footgun now baked into the implementation.
  7. `signal<Event,N>` reentrancy: EnTT's actual `sigh`/`sink` source investigation → found EnTT's real removal mechanism is swap-and-pop (not shift-down), traced a confirmed double-invoke + UB-OOB hazard for detaching not-yet-visited peers, surveyed ETL/QP/Linux-kernel prior art for the mark-and-skip pattern.
  8. `signal<Event,N>` reentrancy: adversarial stress-test of the proposed mark-and-skip design → compiled+ASan/UBSan-verified every reentrancy scenario safe, established the exact attach-during-dispatch deferral contract now documented in `signal.hpp`.
  9. `intrusive_signal<Event>` reentrancy: adversarial stress-test of the proposed singly-linked head-insertion design → confirmed self-detach/detach-other/attach-during-dispatch all safe, but found and precisely characterized a real compound-reentrancy bug (self-detach + detach-own-frozen-successor), now documented as a known caveat rather than engineered around.

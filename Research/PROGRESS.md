# Lw-Eh Progress Ledger

This is the durable, git-tracked ledger for the autonomous build loop. Updated at the end of every firing. Git history is ground truth if this ever drifts — see `git log --oneline`.

- **Phase:** hardening (unchanged)
- **Last completed:** First, retried and confirmed the push of firing 21's commit (`a08b540`) — it had failed four times mid-firing 21 due to genuine, sustained system memory pressure (as low as ~1.1GB free of 15.6GB total, confirmed via `Get-CimInstance Win32_OperatingSystem`), left safely committed-but-unpushed rather than forced through. The retry at the start of this firing succeeded immediately; `origin/main` is caught up. Worth a standing note for future firings: if a push ever fails with an "Out of memory, malloc failed" error, that's very likely this same class of transient host-level memory pressure (git shouldn't need hundreds of MB to push a few KB of diff in a repo this size) — retry once at the start of the next firing before assuming anything is actually wrong with the repo or the commit.
  Then picked up both of firing 21's "next up" candidates:
  1. **Actually ran `-Wl,--print-gc-sections` against the real Xtensa build**, rather than continuing to assume `--gc-sections` works because the flag is set (research.md §B4/§B9's own stated verification step, never previously done). Reconfigured `build/esp32-xtensa` with an ad-hoc `-DCMAKE_EXE_LINKER_FLAGS="-Wl,--print-gc-sections"` override (not committed — a one-off diagnostic, not a permanent flag, since it only adds always-on link-time stderr noise with no behavioral effect), forced a clean relink, and read the real output: exactly 3 sections removed, all Xtensa-specific auxiliary metadata (`.xt.prop` in `startup.S`'s object, `.xt.prop`+`.xt.lit` in the LTO-merged unit) — no function- or data-level code was removed. That absence is expected, not a red flag: this minimal example's `run_scenario()` calls `attach()`+`publish()` for every event type it defines, so there's no genuinely dead code in the binary for `--gc-sections` to catch in the first place (the existing control-vs-with-lweh diff methodology, unchanged since firing 8, already separately proves zero-cost-when-unused). Reverted the override (`cmake -UCMAKE_EXE_LINKER_FLAGS`), rebuilt, reconfirmed the size is back to exactly 375 bytes — the diagnostic left no lasting trace. **Conclusion: `--gc-sections`/`-ffunction-sections` are genuinely active and correctly configured**, now backed by a real, first-time read of the tool research.md specifically named for this exact verification, not just an assumption.
  2. **Found that candidate (b) — "`-mlongcalls` isn't currently set... a completeness gap" — was based on a plain factual error I made writing firing 21's own PROGRESS.md entry, not a real gap.** Read `cmake/toolchains/xtensa-esp32.cmake` directly: `-mlongcalls` has been set via `CMAKE_CXX_FLAGS_INIT`/`CMAKE_C_FLAGS_INIT` (the correct `_INIT`-suffixed form per research.md §B2's own anti-pattern warning about clobbering cache-supplied overrides) since the file's *only* commit, `18a0d5c` — Phase 0's original scaffolding, firing 1. It was never missing at any point in this project's history; I simply didn't check the file before writing that note last firing. No code change needed — recording this plainly (matching how this project has always handled catching its own mistakes, including ones from the immediately preceding firing) rather than quietly dropping the incorrect claim.
- **Current measured size:** Unchanged — **375 bytes incremental** for 5 event types on real ESP32 Xtensa hardware, reconfirmed twice this firing (once after the diagnostic-flag revert). This firing made no source or permanent build-config changes.
- **Test status:** Not re-run this firing — no library, test, or example source changed; only a temporary, reverted linker-flag diagnostic and a documentation correction. Last known-good full run remains firing 21's: 7/7 pass under both `host-debug` and `host-asan`.
- **Next up:** open. Both of firing 21's specific candidates are now resolved (one genuinely verified, one revealed to be a non-issue) rather than carried forward. The smaller "audit research.md's B4/B9 checklist against the current codebase" vein that's been productive for firings 20-22 is likely close to exhausted — remaining B4 items not yet specifically checked (`-fno-jump-tables`, `-mstrict-align`) are either clearly inapplicable to this codebase's shape (no `switch` statements in any dispatch loop; AArch64-specific) or already covered. Next firing should do fresh orientation rather than assume there's more low-hanging fruit in this specific vein. Standing candidate, unchanged: RISC-V/ARM/AVR toolchains remain the largest unresolved gap, needs explicit user permission to install and isn't being sought since it isn't blocking anything above.
- **Open questions for the user:** none blocking. Same standing note: RISC-V/ARM/AVR toolchains not present on this machine; installing one is the user's call, not something this loop will do unilaterally.
- **Subagents launched this project so far:** 11 total (unchanged — this firing was direct diagnostic verification and a documentation correction, no fresh design question needed a subagent).
  1. C++ design/architecture research for a tiny event system → became research.md Part A.
  2. CMake/compiler-flags research for embedded zero-dependency builds → became research.md Part B.
  3. Phase 0 library header/API architecture proposal → adopted into ARCHITECTURE.md.
  4. Phase 0 CMake/test/tooling architecture proposal → adopted into ARCHITECTURE.md (one suggestion rejected).
  5. `delegate<Sig>` toolchain/GCC-version portability investigation → confirmed safe; flagged avr-gcc 7.3.0 as lowest-margin (still safe); flagged ICF folding isn't automatic on Lw-Eh's target toolchains without `-flto`/gold/lld.
  6. `delegate<Sig>` EnTT cross-validation / language-correctness investigation → confirmed the two-overload `bind` design; surfaced the `std::invoke` footgun now baked into the implementation.
  7. `signal<Event,N>` reentrancy: EnTT's actual `sigh`/`sink` source investigation → found EnTT's real removal mechanism is swap-and-pop (not shift-down), traced a confirmed double-invoke + UB-OOB hazard for detaching not-yet-visited peers, surveyed ETL/QP/Linux-kernel prior art for the mark-and-skip pattern.
  8. `signal<Event,N>` reentrancy: adversarial stress-test of the proposed mark-and-skip design → compiled+ASan/UBSan-verified every reentrancy scenario safe, established the exact attach-during-dispatch deferral contract now documented in `signal.hpp`; kept permanently standing via the `host-asan` preset since firing 21.
  9. `intrusive_signal<Event>` reentrancy: adversarial stress-test of the proposed singly-linked head-insertion design → confirmed self-detach/detach-other/attach-during-dispatch all safe, but found and precisely characterized a real compound-reentrancy bug (self-detach + detach-own-frozen-successor), now documented as a known caveat rather than engineered around.
  10. `signal_core` deferral pressure-test (firing 19): given real firing-16/17/18 measured data, does the Phase-0 amendment's own revisit trigger apply now? → no, budget headroom is comfortable; recommended deferring the full rewrite while flagging `attach()`'s uncompensated duplication as real.
  11. Narrower `attach()`/`detach()`-only shared-core design investigation (firing 19): can the Event-agnostic slot-scan be factored out without the full event-erasure adapter-stub cost the original design needed? → yes, a `slot_view`-based runtime-count finder is technically sound (with AVR Harvard-architecture and strict-aliasing caveats addressed); also identified the per-(Event,MaxListeners,Fn) duplication axis; proposed the noinline experiment as a cheaper first test, which the loop then ran directly and found to regress size by 62 bytes, decisively ruling it out.

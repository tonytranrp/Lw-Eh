# Lw-Eh Progress Ledger

This is the durable, git-tracked ledger for the autonomous build loop. Updated at the end of every firing. Git history is ground truth if this ever drifts — see `git log --oneline`.

**Format note (firing 37):** `Last completed` holds only the most recent firing's summary, not a running narrative — every prior firing's full detail (what was found, why, what was verified, exact commands/output) is preserved verbatim in its own commit message(s), which is where cross-references and future digging should go. This entry had grown to four stacked multi-paragraph essays (firings 34-36) before this trim; nothing in them was lost, they're all still in `git log`.

- **Phase:** hardening
- **Format note (this firing):** trims the narrative section again — 5 more entries had re-accumulated since the last trim (a pattern worth naming: this ledger seems to need this roughly every 5-8 new bullets; not a problem, just the expected rhythm of a format that favors verbose-in-the-moment over pre-optimized-terse). Nothing lost; commit hashes below have the full detail.
- **Last completed:** A real ESP32 board enabled genuine on-silicon validation for the first time (firings 38-46). Getting `examples/esp32_minimal` to boot and run correctly required finding and fixing 4 real, hardware-only bugs uncatchable by host tests or static analysis alone: IRAM0's word-only-access restriction breaking byte-accessed `.bss`/`.data`/`.rodata` globals (commits `e438462`, `ac83160`); a genuine `esptool.py elf2image` limitation — it never implements a linker script's `AT>` LMA/VMA split, corrupting every non-zero `.data` global via a copy loop reading from an address nothing was ever written to (commit `1d40989`); and a `call0`-into-windowed-ABI return crash plus the RTC watchdog it unmasked, both fixed and independently ISA-level-cross-validated against real Xtensa ISA pseudocode (commits `b6db5a7`, `38ca015`).
  Since then, on real hardware: `detach()` (commit `732ed60`) and the capacity-boundary rejection path (commit `52ac413`) were both found — via a systematic codegen deep-verification sweep — to have zero real-Xtensa-codegen evidence and are now confirmed working. Every core API surface and documented boundary condition Lw-Eh has now genuinely executed on real silicon; that same sweep also reconciled the incremental size figure exactly, byte-for-byte.
  Then, three independent fresh-clone reproducibility checks (never previously done — every earlier verification reused long-lived `build/` directories): (1) a clean host-debug clone surfaced a real, 100%-reproducible `CMAKE_C_COMPILER` warning present since Phase 0 (`project()` never enables C, but 4 host presets set it anyway) — fixed, all 4 host presets now configure with zero warnings, 7/7 tests still pass fresh; (2) a clean ESP32 clone, built and flashed following only what `ARCHITECTURE.md` documents, reproduced the full real-hardware boot log byte-for-byte (`LWEH BOOT`/`OK`/`DETACH OK`/`CAPACITY OK`), and a size-audit-enabled clone independently reproduced the 888-byte figure exactly; (3) `packaging_check/`'s `find_package(LwEh)` path — never once run locally, only in CI's Ubuntu/GCC — confirmed working end-to-end on this machine's Windows/Clang setup too, a genuinely different platform than CI ever tests.
  Also landed: `examples/esp32_minimal/flash.py` (tested, reusable, now documented in `README.md`); a full core-library + test-suite re-audit (zero defects); a full `research.md` re-read (still accurate); a re-examination of the `signal_core` deferral's own revisit trigger (still not crossed, stays deferred) and an extension of the stable-idle real-hardware claim to 120+ continuous seconds (reproduced twice); an `integrations/esp-idf/` cross-check against research.md §B3 (matches exactly); and forward-looking ESP32-bug cross-references added to the RISC-V/ARM/AVR placeholder examples for whenever those toolchains arrive (two subagents confirmed which bugs would/wouldn't transfer, surfacing one genuinely new gap in the ARM placeholder's vector-table setup along the way).
- **This firing: two small completeness checks on the packaging/CI surface, both clean, no code changed.** (1) Root `CMakeLists.txt`'s `FILE_SET HEADERS` file list (the exact set `install()`/`find_package(LwEh)` exports) was diffed against the actual contents of `include/lweh/` — exact match, all 7 real headers listed, nothing stray, nothing missing; a real way this could have silently regressed (a new header added without updating the list) hasn't happened. (2) Read `.github/workflows/ci.yml`'s `hermeticity-lint` job in full for the first time this session and checked its grep scope (`CMakeLists.txt cmake/ examples/ integrations/ size_audit/ tests/`) against every `CMakeLists.txt`/`*.cmake` file actually in the repo — exhaustive, the only file outside that scope is `packaging_check/CMakeLists.txt`, which the job's own comment says is deliberately excluded (a legitimate `find_package` consumer, not Lw-Eh's own build resolving a dependency) — confirmed that exclusion is real and doesn't also accidentally leave some other directory unchecked.
- **This firing: checked real GitHub Actions CI run results directly for the first time this session — every prior CI belief rested on reading the yaml, never on reading what CI actually produced.** `gh run list` showed 15/15 recent runs green, including the exact current `HEAD` commit at the time. Went further and grepped actual CI log output (not just the workflow file) across all 5 job instances: zero real compiler warnings anywhere, including under real GNU GCC 13.3.0 on Ubuntu (`host-gcc`) — something impossible to verify on this dev machine, whose own `g++` is a Clang alias. Did surface one genuine, real, previously-unnoticed issue: every single job logged `##[warning]Node.js 20 is deprecated... forced to run on Node.js 24` from `actions/checkout@v4`. Confirmed the current stable release is `v7.0.0` and bumped all 3 usages in `ci.yml`; pushed and watched the resulting real run complete — all 5 jobs green, and the deprecation warning is confirmed gone from the new run's logs (not assumed fixed from the version bump alone).
- **This firing: checked the repo's broader GitHub-hosted state (history, issues, PRs, public metadata) for the first time — mostly clean, but one real, flagged-not-fixed finding.** `gh run list` across the full history (67 runs) shows zero non-success runs ever — no hidden red run anywhere. Zero open or historical issues/PRs — confirms this is genuinely solo-maintained with nothing sitting unaddressed in GitHub's own tracker. But `gh repo view` surfaced a real, public-facing issue this loop should not silently fix: the repo's GitHub description reads **"Light weight Event handlers for small circuts."** — a genuine typo (circuts → circuits) and noticeably less precise than the project's own established self-description (`library.json`'s: "Light-Weight Event Handler: a from-scratch, zero-dependency, header-only C++ event/signal dispatch library targeting a 1-2KB compiled footprint on embedded targets."). No repository topics/tags are set either (`repositoryTopics: null`), which is a real discoverability gap. Editing a repo's GitHub-level description/topics is publishing/modifying public content (distinct from committing to the git repo, which this loop already has standing authorization for) — flagged here rather than acted on; see Open questions below for the exact proposed fix, ready to apply on a one-line go-ahead.
- **This firing: compiler warnings had never been checked at all — grepped every `CMakeLists.txt`/`*.cmake` file and `research.md` for `-Wall`/`-Wextra`, found zero matches anywhere, a genuinely unexamined axis.** Compiled every test file plus the ESP32 example (which exercises all 5 real library headers) under `-Wall -Wextra -Wpedantic -Wshadow -Wconversion -Wsign-conversion` on both compiler families this machine can test — host Clang 22.1.4 and real `xtensa-esp32-elf-g++` 8.4.0 (GNU, the actual production family per research.md §B7) — before changing anything. The library headers themselves were already 100% clean on both; one real (harmless) `-Wshadow` hit turned up in `tests/capacity_boundary_test.cpp` (an inner-scope `signal<dummy_event>` named `s` shadowed the outer `signal<dummy_event,2>` also named `s`) — renamed to `s_default`, matching what the surrounding comment already called it. Added this exact verified-clean flag set to `lw_eh_size_flags` (`cmake/LwEhAggressiveFlags.cmake`), deliberately without `-Werror` (would make build success depend on avr-gcc/arm-none-eabi-gcc versions never installed here — a harmless new warning on an unverifiable toolchain would silently become a hard failure; revisit once those toolchains exist to check against). Full re-verification after the change: `host-debug` and `host-asan` both clean-reconfigured, rebuilt with zero warnings, 7/7 pass on each; ESP32 Xtensa clean-reconfigured, rebuilt with zero warnings, and `size_report.py` reproduced **888 bytes incremental exactly** — confirming these are genuinely diagnostic-only, zero codegen/size impact, not assumed. Pushed and watched the real CI run (`0bc5690`) complete — all 5 jobs green, and grepped its full log directly for "warning": zero real compiler warnings anywhere, including `host-gcc`'s genuine Ubuntu GCC (a newer, different version than the 8.4.0 Xtensa proxy available locally) — the only matches were `actions/checkout`'s own unrelated git-init hint, confirming this is clean on every toolchain this project can currently test, not just the ones checked locally.
- **Current measured size:** **888 bytes incremental** for 6 event types on real ESP32 Xtensa hardware (`xtensa-esp32-elf-gcc` 8.4.0, `-Os -flto -flto-partition=none` + full aggressive flag set), confirmed both statically (`size_audit/size_report.py`) and via actual flash+boot (`LWEH CAPACITY OK` on real silicon). Comfortably within the 1-2KB budget for 6 event types per research.md A10.
- **Test status:** 7/7 pass locally under `host-debug`/`host-asan`, clean-reconfigured fresh after the new warning-flags change (a shared-flag-set change, treated as big/structural per the proportionality rule, not spot-checked). `host-gcc` unverifiable locally (this machine's `g++` is a Clang alias) but confirmed green on real CI for the warning-flags commit itself (`0bc5690`), not just an earlier commit — directly watched that run complete and grepped its logs, per the entry above.
- **Next up:** genuinely open, on the most complete footing yet — every core API surface plus the capacity-boundary edge case has run correctly on real hardware, on top of source-level, test-suite, codegen, research-doc, packaging, and CI-infrastructure checks all coming back clean. The project is at a natural completion point for everything achievable without new toolchain access. The one remaining substantive gap — RISC-V/ARM/AVR (and real ESP-IDF) toolchains have never been installed, only source/config-reviewed — stays correctly gated on the user's explicit go-ahead (large downloads, PATH/environment modification), not something this loop can or should decide unilaterally: if/when approved, that's the highest-value next unit of work by a wide margin; absent that, this loop keeps watching for anything genuinely new rather than manufacturing busywork in an already-complete area.
- **Open questions for the user:** none blocking, plus one small, low-stakes, ready-to-apply item this loop deliberately did not act on unilaterally (public GitHub metadata, not a repo file): the repo's GitHub description has a typo — currently **"Light weight Event handlers for small circuts."** — proposed fix: **"Light-Weight Event Handler: a from-scratch, zero-dependency, header-only C++ event/signal dispatch library for embedded targets."** (matching `library.json`'s own wording), optionally with topics like `cpp`, `embedded`, `esp32`, `header-only`, `event-driven` added for discoverability. Say the word and this loop will apply it via `gh repo edit`; otherwise it's a nice-to-have, not blocking anything. Standing notes carried forward: the machine's global `http.postbuffer=500MB` git setting can still cause push OOM errors under concurrent memory pressure (workaround: per-command `-c http.postbuffer=` override, documented in durable memory, never edit the persisted config); RISC-V/ARM/AVR toolchains not present on this machine, installing one is the user's call.
- **Subagents launched this project so far:** 25 total.
  1. C++ design/architecture research for a tiny event system → became research.md Part A.
  2. CMake/compiler-flags research for embedded zero-dependency builds → became research.md Part B.
  3. Phase 0 library header/API architecture proposal → adopted into ARCHITECTURE.md.
  4. Phase 0 CMake/test/tooling architecture proposal → adopted into ARCHITECTURE.md (one suggestion rejected).
  5. `delegate<Sig>` toolchain/GCC-version portability investigation → confirmed safe; flagged avr-gcc 7.3.0 as lowest-margin (still safe); flagged ICF folding isn't automatic on Lw-Eh's target toolchains without `-flto`/gold/lld.
  6. `delegate<Sig>` EnTT cross-validation / language-correctness investigation → confirmed the two-overload `bind` design; surfaced the `std::invoke` footgun now baked into the implementation.
  7. `signal<Event,N>` reentrancy: EnTT's actual `sigh`/`sink` source investigation → found EnTT's real removal mechanism is swap-and-pop (not shift-down), traced a confirmed double-invoke + UB-OOB hazard for detaching not-yet-visited peers, surveyed ETL/QP/Linux-kernel prior art for the mark-and-skip pattern.
  8. `signal<Event,N>` reentrancy: adversarial stress-test of the proposed mark-and-skip design → compiled+ASan/UBSan-verified every reentrancy scenario safe, established the exact attach-during-dispatch deferral contract now documented in `signal.hpp`; kept permanently standing via the `host-asan` preset since firing 21, and via CI since firing 28.
  9. `intrusive_signal<Event>` reentrancy: adversarial stress-test of the proposed singly-linked head-insertion design → confirmed self-detach/detach-other/attach-during-dispatch all safe, but found and precisely characterized a real compound-reentrancy bug (self-detach + detach-own-frozen-successor), now documented as a known caveat rather than engineered around.
  10. `signal_core` deferral pressure-test (firing 19): given real firing-16/17/18 measured data, does the Phase-0 amendment's own revisit trigger apply now? → no, budget headroom is comfortable; recommended deferring the full rewrite while flagging `attach()`'s uncompensated duplication as real.
  11. Narrower `attach()`/`detach()`-only shared-core design investigation (firing 19): can the Event-agnostic slot-scan be factored out without the full event-erasure adapter-stub cost the original design needed? → yes, a `slot_view`-based runtime-count finder is technically sound (with AVR Harvard-architecture and strict-aliasing caveats addressed); also identified the per-(Event,MaxListeners,Fn) duplication axis; proposed the noinline experiment as a cheaper first test, which the loop then ran directly and found to regress size by 62 bytes, decisively ruling it out.
  12. `requires`-clause technical correctness investigation (firing 23): exact syntax, C++17 fallback exactness, binary-size impact, and interaction with the existing `is_member_pointer_v` static_assert → syntax confirmed correct and verified empirically against real clang compilation, but found `is_invocable_r_v` has its own INVOKE-based loophole matching the file's own documented `std::invoke`-avoidance rationale, and that a `requires`-clause failure pre-empts/hides the friendlier body `static_assert` message via SFINAE — directly shaped the final implementation.
  13. Concept-constraint value/minimalism evaluation (firing 23): is this worth doing at all, benchmarked explicitly against firing 19's `signal_core` deferral precedent → no, recommended a plain C++17 `static_assert(is_invocable_r_v<...>)` instead of `requires`, and removing `LWEH_HAS_CONCEPTS` as dead cruft with no other plausible future use found.
  14. Compile-fail testing mechanics investigation (firing 24): what's the correct, robust way to test "this snippet must fail to compile with this message" on this exact CMake/Ninja/Clang/Windows setup → found `try_compile()` has a real, empirically-confirmed header-dependency-tracking gap that would have silently broken the whole mechanism; delivered a complete, tested, ready-to-use alternative (direct compiler invocation via a `cmake -P` driver), not used to build permanent infrastructure but its verified compiler-invocation recipe was reused directly for that firing's one-time manual verification.
  15. Compile-fail testing value evaluation (firing 24): is permanent CTest infrastructure justified for 3 narrow, zero-incidence checks → no, benchmarked against both the `host-asan` precedent (worth building) and the `signal_core` precedent (correctly deferred) and landed closer to the latter; recommended a one-time, dated, recorded verification instead, which the loop then performed for real.
  16. GCC 8.4.0 IPA-ICF vtable-folding-asymmetry investigation (firing 34): given real disassembly showing byte-identical `on_event()` overrides folded to one address while byte-identical `invoke_erased()` overrides did not, in the same binary — why the difference? → built a faithful repro on the real toolchain with `-fdump-ipa-icf-details`, confirmed empirically (via the dump's `"Equals called for:"` log lines, not guessed): ICF hash-buckets candidates before ever comparing bodies, `on_event`'s instantiations hash identically and fold, `invoke_erased`'s hash differently and never reach body comparison, because its call to `on_event` is polymorphic/indirect (hashed more richly, type-leaking) vs. `publish()`'s direct call to `dispatch()` (hashed thin, folds fine) — a clean internal control group ruled out simpler theories. Not fixable without collapsing the erasure-thunk into the listener's own virtual method (a real architecture tradeoff for ~tens of bytes); no code change made, matches pre-registered scoping.
  17. ESP32 image/flashing mechanics investigation (firing 38): given real hardware + esptool.py + prebuilt Arduino-ESP32 bootloader/partition files but no ESP-IDF/pio CLI, what's the exact `elf2image`/`write_flash` recipe to produce a genuinely bootable image, and critically whether the second-stage bootloader hard-requires an ESP-IDF-style app-descriptor header a from-scratch app wouldn't have → no descriptor required (confirmed via `nm` on the real local bootloader ELF, the checking code isn't even linked in); full verified recipe + a real landmine caught (stale prebuilt `default.bin`) delivered, see commit `f4a09c6`.
  18. Real-hardware startup-code correctness investigation (firing 38): does `scenario.hpp`'s global `signal<>`/`intrusive_signal<>` object graph actually need `.bss` zeroing/`.data` copy-down/C++ static-init to behave correctly on real silicon (not just "is it bad practice"), and is the current linker script's flat IRAM-only memory layout structurally viable for real execution → zero dynamic init needed (confirmed via `nm`), but the flat layout is genuinely broken (a real byte-store to IRAM0 confirmed via disassembly, would fault on real silicon); built, linked, and verified a fix (real IRAM0/DRAM0 split, LMA≠VMA `.data`). Implemented and independently re-verified, landed in commit `e438462`.
  19. Real-hardware bootloader boot-loop diagnosis (firing 38): the flash succeeds completely (all 4 components hash-verified) and the ROM correctly loads the prebuilt second-stage bootloader, but it hangs silently before producing any log output and the RTC watchdog resets it in a loop — why? → in flight, result pending; has direct access to the real hardware to test hypotheses, not just reason abstractly.
  20. ESP32 bare-metal UART0 TX register investigation (firing 39): exact base address/FIFO/status-register layout for polled UART0 TX, and whether clock/GPIO-matrix/baud pre-init is required from a cold from-scratch entry point (no SDK) → base `0x3ff40000`, FIFO at `+0x0`, status/`TXFIFO_CNT` at `+0x1C` bits `[23:16]`, `CLKDIV` at `+0x14` (all cited to real local `soc/*_reg.h` headers, cross-checked via `nm` against the real bootloader ELF's own `UART0` absolute symbol — exact match); no pre-init needed, UART0 is clocked/pin-routed/~115200-8N1 from power-on reset on this specific boot path (ROM + bootloader already rely on exactly this). Implemented directly, verified via objdump/literal-pool decoding against the real built ELF.
  21. ESP32 bootloader silent-hang root-cause research (firing 40): given the real captured boot log (loads, jumps to entry, ~9s silence, RTC watchdog reset, repeats), what does real ESP-IDF v4.4.7 bootloader source do between entry and its first log line, and what's the most likely from-scratch-flashing-specific cause → found this exact prebuilt bootloader's logging is entirely compiled out (`CONFIG_BOOTLOADER_LOG_LEVEL_NONE=y`), so silence alone was never diagnostic; the ~9s timing matches the watchdog's fresh 9000ms re-arm partway through `bootloader_init()`, meaning the hang is later than a naive reading suggested; ranked SPI flash-mode mismatch (image header consumed at the flash-init step) as the leading cause. Independently reproduced and confirmed locally (no hardware touched): `elf2image` on `bootloader_dio_40m.elf` without an explicit `--flash_mode` defaults to `QIO` against DIO-only board wiring, with an entry-point address matching the real captured boot log exactly; regenerating with explicit `--flash_mode dio --flash_freq 40m --flash_size 4MB` produces a verified, materially different, ready-to-test image. Not yet tried on real hardware — COM5 was actively held by a separate concurrent session throughout.
  22. Xtensa `entry`/`retw` ISA-level cross-validation (independent thread, before commit `b6db5a7` landed): is "`app_main()` never returns" a genuine architectural fix for the `call0`-into-windowed-ABI return crash, or just symptom-masking? → genuine fix, confirmed via real ISA-level citations (Espressif's own `WindowedOption.tex` pseudocode, ESP32's actual `core-isa.h`/`xtensa_vectors.S`): `entry` unconditionally rotates the register window from whatever `PS.CALLINC` holds with no check a matching windowed `callX` preceded it, and `call0` never sets `PS.CALLINC` — so this is architecturally the *only* broken transition in the whole call graph, not one of several. Real window-exception-vector setup was evaluated and deliberately not pursued (nothing needs `app_main` to return). Landed via commit `38ca015`.
  23. Systematic ESP32 codegen deep-verification sweep (this session): first full struct-layout/RTTI/ICF/gc-sections check on the compiled binary since firing 34, predating the entire real-hardware saga — is there anything the disassembly shows that source-level reasoning missed? → all clean, real `objdump`/`readelf`/`nm` evidence throughout: `delegate` confirmed exactly 8 bytes/zero padding on the real Xtensa ABI; `intrusive_core::dispatch()` confirmed a genuinely shared symbol across event types via real `call8` call sites; zero RTTI/exception artifacts leaked in despite the aggressive flag set; ICF folding behaves exactly as firing 34 characterized (reconfirmed with a new event-type pairing); `gc-sections` left nothing suspicious; the incremental size figure reconciled exactly, byte-for-byte, across every section. This is the investigation that flagged `detach()`/`unlink()` as never having been compiled into any ESP32 binary, directly motivating the detach() and capacity-boundary hardware-confirmation work that followed (commits `732ed60`, `52ac413`).
  24. ARM Cortex-M bug-transfer investigation: do any of the four real ESP32-Xtensa hardware bugs (IRAM0 word-only access, esptool.py's `AT>`-blind `elf2image`, call0-into-windowed-ABI, RTC watchdog) apply to the Cortex-M placeholder, and is anything else architecturally relevant? → none of the four transfer (confirmed via real ARM docs: Code region is byte-accessible, standard flashing tools honor `AT>`/LMA correctly, no windowed-register concept exists) — but surfaced a genuinely more fundamental gap unprompted: the placeholder's `ENTRY(Reset_Handler)` only sets ELF metadata; real hardware needs an actual `.isr_vector` array at address 0, which doesn't exist yet. Landed as a comment in `cortex_m.ld`.
  25. AVR bug-transfer investigation: same question for the AVR placeholder → none of the four transfer either (true Harvard split via separate instructions rather than a permission bit, `PROGMEM` confirmed unused anywhere in this repo, avr-libc's crt1 + avr-gcc's own linker script are decades-proven, no windowed-register concept), but surfaced AVR's own non-Xtensa watchdog gotcha (avr-libc FAQ: WDT can survive a reset at a short timeout) worth flagging, plus confirmed the project's existing ISR/concurrency-out-of-scope policy (research.md §A9) bites harder in practice on AVR's 8-bit-native width. Landed as a comment in `avr_minimal/main.cpp`.

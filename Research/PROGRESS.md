# Lw-Eh Progress Ledger

This is the durable, git-tracked ledger for the autonomous build loop. Updated at the end of every firing. Git history is ground truth if this ever drifts — see `git log --oneline`.

- **Phase:** embedded-validation (both storage policies now have real embedded cross-compile data; RISC-V/ARM/AVR remain unvalidated)
- **Last completed:** Extended `examples/esp32_minimal/scenario.hpp` (the shared "one source of truth for realistic usage" fixture, per `Research/ARCHITECTURE.md`) with a third event type, `connection_event`, dispatched via `intrusive_signal<Event>`/`intrusive_node<Event>` (a `connection_logger` class inheriting `intrusive_node<connection_event>`) — previously only `signal<Event,N>` (the array-based policy, `button_event`/`sensor_reading_event`) had any embedded validation at all; the intrusive policy had none. Used a genuinely distinct third event type rather than adding a second storage policy to an existing one, so the two policies read as separate, independently understandable examples rather than a confusing "why does this event have two signals" case. Updated both consumers of the shared scenario (`examples/esp32_minimal/main.cpp`'s `app_main()`, `size_audit/with_lweh_main.cpp`'s host-proxy `main()`) for the new 3-argument `run_scenario()` signature. Verified in the order the loop's own proportionality rules call for: host sanity build+test first (library headers themselves untouched this firing, so this was correctly the fast path — 7/7 pass, Ninja "no work to do" on the library), then the host-proxy `size-host` preset build (compiles clean), then — now that last firing unblocked it — a genuine `esp32-xtensa` cross-compile using the toolchain discovered last firing (`PATH="$HOME/.platformio/packages/toolchain-xtensa-esp32/bin:$PATH"`), which built and linked with zero errors on the first attempt (both prior structural fixes — the missing `examples/CMakeLists.txt`, the ASM-language gap, the Xtensa linker-script `.literal` placement bug — held up correctly under this new, larger scenario; nothing new broke).
- **Current measured size:** Real Xtensa measurement via `size_report.py` (unmodified since firing 6): **520 bytes incremental** for all three event types combined (previously 372 bytes for just the two `signal<Event,N>`-based ones) — meaning the third event type's marginal cost, via `intrusive_signal`, is **148 bytes**. `nm` on the genuine ELF gives a concrete, real-hardware confirmation of the exact storage-policy tradeoff research.md §A7 documents and firing 5's host-side symbol investigation already suggested: `g_connection_signal` (the `intrusive_signal` instance itself) is **4 bytes** — literally just the one `head_` pointer — dramatically smaller than `g_button_signal`/`g_sensor_signal`'s 32 bytes each (a 4-slot `delegate` array), but the *listener* now carries real cost that the array-based design doesn't impose on listeners at all: `vtable for connection_logger` = 16 bytes, `g_connection_logger` (the listener object: vptr + inherited `next` pointer + its own `bool` state) = 12 bytes, plus the unavoidable per-type `invoke_erased` adapter = 16 bytes. This is "cost shifted onto the listener, not the dispatcher" (research.md's own words for this tradeoff) made concrete and measured on real hardware rather than just asserted. 520 bytes for three event types across both storage policies remains comfortably within the 1-2KB budget — roughly half the low end, with real headroom for more event types before any concern. Standing caveats unchanged: RISC-V/ARM/AVR still unvalidated; `bloaty` still not installed on this machine.
- **Test status:** 7/7 pass (`ctest --preset host-debug`, 2026-07-16) — unchanged; this firing's library-adjacent-but-not-library changes (example/probe wiring only) correctly didn't need new test coverage, and the existing suite's re-run was the fast/small path per the loop's proportionality rule (nothing in `include/lweh/*.hpp` changed).
- **Next up:** open choice, similar shape to recent firings: (a) `README.md` is still the original two lines predating this project's real work — genuinely the longest-deferred, lowest-risk increment on the table at this point (flagged as an option two firings running now); or (b) revisit whether `bloaty` is installable the same way the Xtensa toolchain turned out to already be present (check common install locations / already-downloaded-but-unindexed tool caches before assuming it needs a fresh install) to round out `size_report.py`'s diff-mode output; or (c) push deeper embedded validation — e.g. actually reading the linker's `-Wl,--print-gc-sections` output (or the `.map` file) on the real Xtensa build to confirm dead-code elimination is doing what's expected, matching the loop's own "codegen deep-verification" step, which hasn't been exercised on real hardware output yet, only reasoned about. Leaning toward (a) given how long it's been deferred, but no strong preference.
- **Open questions for the user:** none blocking. Same standing notes as last firing (RISC-V/ARM/AVR toolchains not found on this machine; would extend validation to the other claimed target families if available).
- **Subagents launched this project so far:** 9 total (unchanged this firing — extending an already-validated pattern to a third event type didn't raise a fresh design question needing investigation).
  1. C++ design/architecture research for a tiny event system → became research.md Part A.
  2. CMake/compiler-flags research for embedded zero-dependency builds → became research.md Part B.
  3. Phase 0 library header/API architecture proposal → adopted into ARCHITECTURE.md.
  4. Phase 0 CMake/test/tooling architecture proposal → adopted into ARCHITECTURE.md (one suggestion rejected).
  5. `delegate<Sig>` toolchain/GCC-version portability investigation → confirmed safe; flagged avr-gcc 7.3.0 as lowest-margin (still safe); flagged ICF folding isn't automatic on Lw-Eh's target toolchains without `-flto`/gold/lld.
  6. `delegate<Sig>` EnTT cross-validation / language-correctness investigation → confirmed the two-overload `bind` design; surfaced the `std::invoke` footgun now baked into the implementation.
  7. `signal<Event,N>` reentrancy: EnTT's actual `sigh`/`sink` source investigation → found EnTT's real removal mechanism is swap-and-pop (not shift-down), traced a confirmed double-invoke + UB-OOB hazard for detaching not-yet-visited peers, surveyed ETL/QP/Linux-kernel prior art for the mark-and-skip pattern.
  8. `signal<Event,N>` reentrancy: adversarial stress-test of the proposed mark-and-skip design → compiled+ASan/UBSan-verified every reentrancy scenario safe, established the exact attach-during-dispatch deferral contract now documented in `signal.hpp`.
  9. `intrusive_signal<Event>` reentrancy: adversarial stress-test of the proposed singly-linked head-insertion design → confirmed self-detach/detach-other/attach-during-dispatch all safe, but found and precisely characterized a real compound-reentrancy bug (self-detach + detach-own-frozen-successor), now documented as a known caveat rather than engineered around.

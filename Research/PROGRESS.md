# Lw-Eh Progress Ledger

This is the durable, git-tracked ledger for the autonomous build loop. Updated at the end of every firing. Git history is ground truth if this ever drifts — see `git log --oneline`.

- **Phase:** embedded-validation (unchanged — small, verified flag-set addition)
- **Last completed:** Did the genuinely-fresh, full re-read of `research.md` flagged as overdue for several firings (option (a) from last firing) — read Parts A and B start to finish, not targeted re-skims. Cross-referenced every section against the current implementation: confirmed all 11 of the "Key cross-cutting takeaways" and all of the "open decisions to resolve" from the intro are either correctly implemented (delegate shape, shared-core deferral reasoning, storage-model choice, C++17 floor, hermeticity, RTTI/exception avoidance) or explicitly, deliberately deferred with documented reasoning (ISR-safe queued dispatch, concurrent/multi-core synchronization) — no silent gaps found there. Found one concrete, previously-untried, actionable gap in the compiler/linker flag checklist (B4/B10): `-Wl,--relax`, which research.md documents as having its biggest impact specifically on RISC-V and Xtensa (the exact targets this project cares about) by shrinking conservatively-long call/address sequences once final addresses are known — never added to `cmake/LwEhAggressiveFlags.cmake`. Added it, gated to `LWEH_IS_GNU` only (not the broader `LWEH_IS_GCC_LIKE`) after discovering empirically that this project's host Clang build (via llvm-mingw's bundled `lld`) hard-errors with `"unknown argument: --relax"` rather than accepting or silently ignoring it — confirms `--relax` is a GNU-binutils-`ld`-family flag, not something compiler-ID alone reliably predicts support for, exactly matching research.md's own framing (never trusted blindly, since the loop's own step 3 requirements treat any compiler/linker flag-set change as structural). Also considered adding a permanent `-Wl,-Map=...` to the same shared flag target (research.md §B9's own recommended workflow wants map-file cross-referencing) but caught a real bug in my own first draft before it shipped: `lw_eh_size_flags` is an `INTERFACE` library consumed by 8+ different targets, and the generator expression I'd reached for (`$<TARGET_NAME:lw_eh_size_flags>`) would resolve to the same fixed literal filename for every one of them, not the actual consuming target's name — every test/example/size_audit binary would clobber the same shared `.map` file. Rather than risk a subtly-wrong "current target" generator expression I hadn't verified, dropped that addition entirely and documented why; the map-file workflow stays available on demand via direct manual re-link (as done in firing 10), which is simpler and already proven safe. Verified the `--relax` fix properly: full clean host rebuild (initially failed exactly as predicted with the `lld` error before the GNU-only fix, confirming the bug was real and not hypothetical; passed cleanly after), full `ctest` run (structural flag-set change — big, not small — matches the loop's own proportionality rule), then a full clean real-hardware Xtensa rebuild and re-measurement.
- **Current measured size:** Real Xtensa measurement, unchanged: **520 bytes incremental** (875 with-lweh − 355 control), byte-for-byte identical to firing 8/10's number. `--relax` had zero measurable effect at Lw-Eh's current scale — consistent with firing 10's codegen finding that `main()` is fully inlined with no long-distance call sequences to shorten in the first place; relaxation only has something to do once a binary has call chains long enough to need post-hoc shortening. Kept the flag anyway (documented as currently a no-op, not misrepresented as a win): it's a one-line, zero-cost, already-correctly-scoped addition directly recommended by research.md for exactly this target family, and pays off automatically as the library's real-world usage grows (more event types, deeper call graphs) without needing to be rediscovered later — this is a forward-compatibility argument for a free, harmless, already-researched flag, not speculative abstraction-building.
- **Test status:** 7/7 pass (`ctest --preset host-debug`, 2026-07-16), full suite — correctly judged "big": a compiler/linker flag-set change is explicitly called out by the loop's own rules as requiring the full suite, not a targeted subset.
- **Next up:** open choice: (a) `MaxListeners` capacity is still only demonstrated at its default (4) anywhere in the codebase — flagged as an option for two firings now, still not done; a real test or example exercising a non-default capacity would close this out; (b) the fresh research.md re-read surfaced no other unaddressed gaps, so absent a new one, resume the narrower deep-verification idea from firing 10 (isolate the array-based `signal<Event,N>` path specifically, or check whether `-flto-partition=none` vs. GCC's default partitioned LTO changes anything measurable) as a lower-priority, rounding-out task. Leaning toward (a) since it's a real, user-facing gap in demonstrated coverage rather than a verification nice-to-have.
- **Open questions for the user:** none blocking. Same standing notes as recent firings (RISC-V/ARM/AVR toolchains not found on this machine; `bloaty` confirmed not present).
- **Subagents launched this project so far:** 9 total (unchanged — this firing's work was a direct research.md re-read plus a small, empirically-testable flag addition, neither warranting fresh subagent investigation).
  1. C++ design/architecture research for a tiny event system → became research.md Part A.
  2. CMake/compiler-flags research for embedded zero-dependency builds → became research.md Part B.
  3. Phase 0 library header/API architecture proposal → adopted into ARCHITECTURE.md.
  4. Phase 0 CMake/test/tooling architecture proposal → adopted into ARCHITECTURE.md (one suggestion rejected).
  5. `delegate<Sig>` toolchain/GCC-version portability investigation → confirmed safe; flagged avr-gcc 7.3.0 as lowest-margin (still safe); flagged ICF folding isn't automatic on Lw-Eh's target toolchains without `-flto`/gold/lld.
  6. `delegate<Sig>` EnTT cross-validation / language-correctness investigation → confirmed the two-overload `bind` design; surfaced the `std::invoke` footgun now baked into the implementation.
  7. `signal<Event,N>` reentrancy: EnTT's actual `sigh`/`sink` source investigation → found EnTT's real removal mechanism is swap-and-pop (not shift-down), traced a confirmed double-invoke + UB-OOB hazard for detaching not-yet-visited peers, surveyed ETL/QP/Linux-kernel prior art for the mark-and-skip pattern.
  8. `signal<Event,N>` reentrancy: adversarial stress-test of the proposed mark-and-skip design → compiled+ASan/UBSan-verified every reentrancy scenario safe, established the exact attach-during-dispatch deferral contract now documented in `signal.hpp`.
  9. `intrusive_signal<Event>` reentrancy: adversarial stress-test of the proposed singly-linked head-insertion design → confirmed self-detach/detach-other/attach-during-dispatch all safe, but found and precisely characterized a real compound-reentrancy bug (self-detach + detach-own-frozen-successor), now documented as a known caveat rather than engineered around.

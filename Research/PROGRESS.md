# Lw-Eh Progress Ledger

This is the durable, git-tracked ledger for the autonomous build loop. Updated at the end of every firing. Git history is ground truth if this ever drifts — see `git log --oneline`.

- **Phase:** embedded-validation (unchanged — this firing was host-only test coverage, no library/build code)
- **Last completed:** Picked up candidate (a) from last firing's "next up": audit `signal<Event,N>`'s own test suite the same way `intrusive_signal`'s was audited two firings ago. Read all four relevant files (`attach_detach_test.cpp`, `capacity_boundary_test.cpp`, `dispatch_order_test.cpp`, `reentrancy_test.cpp`) before concluding anything:
  1. **The specific idea last firing proposed** ("confirm a detached slot's index gets reused by a subsequent `attach()`") turned out to already be covered — `capacity_boundary_test.cpp` lines 46-49 explicitly detach-then-reattach and check the result. Ruled out, not a gap; recording so it isn't re-proposed.
  2. **Reading further turned up a real, narrower, structurally-analogous gap to the one found in `intrusive_signal` two firings ago**: no test detaches a true *middle* slot (not first, not last) from a partially-full array **outside of any live dispatch**, then attaches a replacement and confirms it lands in exactly that freed slot with dispatch order reflecting the resulting slot layout. `reentrancy_test.cpp`'s scenario 3 exercises the same underlying first-fit-reuse mechanism, but only entangled with the in-dispatch deferral-timing contract (detach+attach both happen inside a live callback) — never in isolation as a plain, synchronous, outside-dispatch case. Added one test to `dispatch_order_test.cpp` (its natural home, since the concern is dispatch order, not attach/detach success/failure): attach A/B/C into slots 0/1/2, detach B (the true middle) via an ordinary call, attach D, publish, and confirm the order is A, D, C — proving D reused slot 1 rather than being appended after C. Passed on the first try.
  3. **Checked `include/lweh/config.hpp` while in this area** and found `default_max_listeners = 4`, matching the README's documented default. But checking which tests actually exercise the *implicit* default (no explicit second template argument) found only `attach_detach_test.cpp` and `multi_event_isolation_test.cpp` do — and neither ever attaches more than 2 listeners, so neither would fail if the constant were silently changed to, say, 3 or 8. `capacity_boundary_test.cpp` deliberately uses an explicit `signal<dummy_event, 2>` for a tight/fast boundary check, which sidesteps the default's value entirely by design (its own comment says so). **This is a real, distinct gap** — not implemented this firing (it's a different concern from the middle-slot-reuse test above: pinning down a specific documented constant's value, not dispatch-order/slot-reuse mechanics) — handed off as next firing's concrete starting point instead of bundling a second unit of work into this one.
- **Current measured size:** Unchanged — still **520 bytes incremental** on real ESP32 Xtensa hardware (firing 8/10/11). This firing added test-only code (never compiled into the library or any measured artifact), so no re-measurement was needed or taken.
- **Test status:** 7/7 pass (`ctest --preset host-debug`, 2026-07-16), full suite run. Judged worth the full suite despite being a single-file-scoped test addition — same reasoning as the last two firings: touches a shared core mechanism (`signal<>`'s first-fit slot scan), full suite is under a second, cheap insurance.
- **Next up:** concrete, ready to implement: add a test pinning down `default_max_listeners`'s actual value (4) — e.g. attach exactly 4 listeners to a plain `lweh::signal<Event>` (no explicit second template argument) and confirm a 5th is rejected, the mirror image of `capacity_boundary_test.cpp`'s explicit-N=2 test but for the implicit default specifically. After that, the "audit an already-implemented type's test suite for entangled-vs-isolated gaps" vein that's paid off for three firings running (delegate equality, intrusive_signal head/middle detach, signal<> middle-slot reuse+order) is likely close to exhausted for the two storage-policy types — signal<>'s attach/detach is a single uniform linear scan with no other structurally distinct branch left to isolate (confirmed two firings ago), and intrusive_signal's only remaining branch (unlink's two paths) is now both covered. Beyond that: RISC-V/ARM/AVR toolchains remain the standing, largest unresolved gap, still genuinely absent from this machine as of last firing's check — installing one needs explicit user permission and isn't being sought since it isn't blocking anything above.
- **Open questions for the user:** none blocking. Same standing note: RISC-V/ARM/AVR toolchains not present on this machine; installing one is the user's call, not something this loop will do unilaterally.
- **Subagents launched this project so far:** 9 total (unchanged — this firing, like the two before it, was direct investigation-by-reading-the-actual-code-and-tests plus test-writing against already-decided, already-verified semantics; no fresh design question needed a subagent).
  1. C++ design/architecture research for a tiny event system → became research.md Part A.
  2. CMake/compiler-flags research for embedded zero-dependency builds → became research.md Part B.
  3. Phase 0 library header/API architecture proposal → adopted into ARCHITECTURE.md.
  4. Phase 0 CMake/test/tooling architecture proposal → adopted into ARCHITECTURE.md (one suggestion rejected).
  5. `delegate<Sig>` toolchain/GCC-version portability investigation → confirmed safe; flagged avr-gcc 7.3.0 as lowest-margin (still safe); flagged ICF folding isn't automatic on Lw-Eh's target toolchains without `-flto`/gold/lld.
  6. `delegate<Sig>` EnTT cross-validation / language-correctness investigation → confirmed the two-overload `bind` design; surfaced the `std::invoke` footgun now baked into the implementation.
  7. `signal<Event,N>` reentrancy: EnTT's actual `sigh`/`sink` source investigation → found EnTT's real removal mechanism is swap-and-pop (not shift-down), traced a confirmed double-invoke + UB-OOB hazard for detaching not-yet-visited peers, surveyed ETL/QP/Linux-kernel prior art for the mark-and-skip pattern.
  8. `signal<Event,N>` reentrancy: adversarial stress-test of the proposed mark-and-skip design → compiled+ASan/UBSan-verified every reentrancy scenario safe, established the exact attach-during-dispatch deferral contract now documented in `signal.hpp`.
  9. `intrusive_signal<Event>` reentrancy: adversarial stress-test of the proposed singly-linked head-insertion design → confirmed self-detach/detach-other/attach-during-dispatch all safe, but found and precisely characterized a real compound-reentrancy bug (self-detach + detach-own-frozen-successor), now documented as a known caveat rather than engineered around.

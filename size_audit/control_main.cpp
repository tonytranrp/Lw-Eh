// Baseline firmware skeleton — deliberately never includes any lweh header.
// Its measured size is the "platform floor" that size_report.py subtracts
// out of the with-lweh build (research.md §B6, §B9).

extern "C" void app_main() {}
